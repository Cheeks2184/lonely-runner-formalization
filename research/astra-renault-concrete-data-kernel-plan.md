# Frozen Renault5.1 endpoint-data kernel check

Root predeclaration2026-09-06. Implement the exact finite facts in the
accepted concrete bridge manuscript ec29e6cba74dd87a9db823e712fba099d47d6277bf52b2d227ce2d633e0dca60.
The sole data input is certificate JSON c1374409d4f6804487179fe9765695a2f0d756ccd2a79b5ee8616e919acbb7b1.
Copy61 cuts,120 atom masks,41 core atom indices
`class_reps[minimal_classes[i]]`, and120 supplied dom indices in order.
Do not regenerate any mask or witness; this is literal extraction only.

Define natural-indexed cuts using a total default outside the61 entries;
M:Fin120->Nat, core:Fin41->Fin120, dom:Fin120->Fin41 using literal vectors.
Kernel-check the exact PhaseBandFacts structure from RenaultPhaseBandFacts,
not a simplified approximate predicate. Check120 fixed atom rows, each over
23 candidate bits (2760 pairs), with true-bit implications for both singleton
strict/closed remainder tests and open integer endpoint bounds. Rows may be
private ordinary decide lemmas assembled by finite cases. Check first/last
cuts,41 core-mask equalities, and120 domination AND-equalities. Exact defined
objects must appear in the public theorem types; no metadata trust bridge.

The finite domain is fixed. No discovery, candidate search, shrinking,
changing strictness or replacement of the existing41core masks is allowed.
Root must read exact generated source and record its hash before the one
initial complete source invocation. Limits:8GiB virtual AS, CPU59/60seconds,
wall60seconds, per-stream256KiB file caps; maxHeartbeats1000000 and
maxRecDepth4096 per declaration; local Lean threads1/stack65536.
Retain actual child status, all source/output hashes and complete capture;
no automatic cap expansion. Syntax-only corrections preserving all statements
may be rerun at the same caps with failed evidence retained. Source-only
worker drafting does not itself execute or certify these predicates.

These finite facts are still only one part of the real proof. They require
generic band soundness, atom coverage, mask subset transport, and phase
periodicity before the exact Renault5.1 theorem follows. No new runner count
or unrestricted conjecture conclusion is implied by this check.
