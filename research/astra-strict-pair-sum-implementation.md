# Strict pair-sum completeness implementation

`exists_strictWitness_iff_pairSum` takes the attained finite minimum of a
strict witness and applies closed pair-sum completeness at that positive
margin.  It requires distinct labels, not distinct speed values.  Saved source
and named build completed in session `92472`, terminal exit `0` (initial build
session `75285` also exited `0`).  The exact-type and allowed-axiom probe in
session `98427` exited `0`; only `propext`, `Classical.choice`, and
`Quot.sound` were reported.  The probe compiles a strict quarter witness on
repeated `Fin 2` speeds.
