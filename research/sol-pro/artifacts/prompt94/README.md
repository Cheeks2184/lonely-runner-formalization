# Prompt94 recovered artifact quarantine

This directory preserves the single response-linked Prompt94 archive recovered
by the GPT-5.6 Sol High desktop orchestrator. It is retained byte-for-byte for
provenance and a later independently specified replay.

The file under `quarantine/` is **UNVERIFIED / QUARANTINED**. It was not
executed, imported, opened, parsed, listed, or extracted. Its internal member
names, file counts, formats, hashes, completion markers, source/output
bindings, and mathematical claims are unknown to this recovery package.

## Recovered inventory

| File | Bytes | SHA-256 |
| --- | ---: | --- |
| `quarantine/p94_artifacts.tar.gz` | 86,196 | `5d2e16bc120fa84e8cb58319b903700cecf8cd1d431b19adf77f2712c22d49d2` |

The measured archive hash exactly matches the bundle hash printed in the
response. That establishes container identity only. It does not independently
verify any claimed source, executable, output, finite domain, implementation
agreement, or completion result.

The first browser download attempt showed a stale suggested Save-As filename
and was canceled before preservation. A second recovery used the response's
exact attachment link and produced the hash-matching archive above. No file
from the canceled attempt is tracked.

## Response boundary

The normalized response is
`../../responses/response94.md`. It preserves the response's `OPEN`
self-disposition and all mathematical and computational claims without
accepting them. Response reasoning is **RESPONSE-AUTHORED / UNVERIFIED**. The
reported finite computations are **UNVERIFIED / QUARANTINED**, not computed
finite evidence, pending an independently specified clean-room replay.

See `recovery-manifest.txt` for exact byte provenance and
`../../audits/response94-recovery-audit-304.md` for rapid fail-closed triage.
