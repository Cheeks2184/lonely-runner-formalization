# Prompt87 recovery provenance

This directory records the public-safe recovery boundary for the completed
Prompt87 Sol Pro response. Prompt87 returned no attached verifier, output, or
other file. The raw response contains no `sandbox:/...` reference, Markdown
link, or attachment/download marker, and no separate attachment accompanied
the authoritative capture. Recovery therefore packages only the normalized
response and this provenance memo.

## Exact response provenance

The immutable raw clipboard capture has SHA-256
`b76d65f00d4c7b0acd6ed6a2e8c2c493025b31a914299a4f3a7556a84a3a9d4d`:
35,511 UTF-8 bytes, 35,497 characters, 1,034 CRLF separators, no lone LF or
CR, and a terminal CRLF. Strict UTF-8 decoding found no NUL or replacement
character.

The tracked response is exactly that capture with the repository's required
CRLF-to-LF normalization and no other byte or whitespace change. It has
SHA-256
`228e51affc27c39981cdf3f5f086a17d2ddcef2d1c1a528c74a9ca281a566d5c`:
34,477 UTF-8 bytes, 34,463 characters, 1,034 LF separators, no CR, and a
terminal LF. Re-expanding each tracked LF to CRLF reproduces the immutable raw
capture byte-for-byte.

The response self-disposition is **PIVOT**. It presents response-authored
arithmetic, a dyadic counterfamily audit, and an aggregate dyadic
cover-divisibility lemma as a surviving open edge. Those are response claims
pending separate mathematical and computational audit; recovery does not
accept them.

## Prompt and launch binding

The launch record names integrated prompt commit
`1cc4371a6df1036d54a3580cde5eb73f651eecb7`, which is an ancestor of recovery
base `1736d5bad1652d40926b9939719f47dfb5b80da8`. The tracked prompt independently
recomputes to SHA-256
`0a9456c26579ee3e092b8d5e73dae16334226f4ed482d43759a049df8617bbff`:
13,860 UTF-8 bytes, 13,836 characters, 353 LF separators, and a terminal LF.
Removing only that terminal LF gives the recorded submitted payload SHA-256
`14dfdb0f18c05ee9cb34686fe1e328d3cb82b91774cb4d7719ae3315572cdf06`.
These values exactly match the pre-existing Prompt87 launch provenance.

The response was captured at `2026-08-04T02:06:57.4791204-05:00`
(`2026-08-04T07:06:57.4791204Z`), 35 minutes 9.4274131 seconds after the
recorded launch observation. This elapsed interval is lifecycle provenance,
not a mathematical or performance claim.

## Evidence and privacy boundary

No response-authored calculation, finite scan, modular enumeration, symbolic
proof, counterfamily, ordinary-pivot calculation, implication chain, or
falsification was executed or replayed during recovery. No theorem,
counterexample, computed finite evidence, Lean result, bridge, or LRC status
is promoted by this package. The aggregate dyadic lemma and unrestricted
Lonely Runner Conjecture remain open pending independent review.

A targeted credential, private-key, browser-session, personal-email,
connection-string, and private-machine-path scan found no sensitive artifact.
A deliberately broad drive-path expression matched five TeX fragments such as
`i:\rho`; manual inspection confirmed that all five are mathematical notation,
not filesystem paths or machine identifiers.

`git diff --check` reports no exception for the immutable response or this
provenance memo. No response line was edited to silence a whitespace or
merge-marker diagnostic.
