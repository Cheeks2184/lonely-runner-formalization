---
owner: GPT-5.6 Sol High top-level desktop orchestrator
supervising_authority: /root
launch_runtime: original browser-capable desktop Codex session
role: Research Pipeline Lead - prompt preparation only
task_id: PIPE-P77-PRIMITIVE-SIGNED-GRAM-CONTRACT-180
base_commit: 94442b430f4dbb9b66ec186e5655d556ee4e477e
admission: LAUNCH-READY
---

# Prompt 77: PRIMITIVE-DEGREE-2N-SIGNED-GRAM-GAP

This is one isolated unrestricted Sol Pro research contract. Prove or
decisively refute the exact primitive signed-character bridge below. It is a
direct all-pivot Fourier/additive-energy route, independent of Prompt67 prime
forcing and Prompt76 canonical-private-deficit Hall transport.

This file is a prepared contract only. It is not a browser launch, active
research cell, result, audit, or status promotion. Only GPT-5.6 Sol High under
`/root`, using the original signed-in browser-capable desktop session, may
start a genuinely new ChatGPT conversation, explicitly select GPT-5.6 Sol
with Pro effort, submit this contract, recover the response, and decide its
status. Sol Medium prepared the specification only. Luna may later replay one
frozen artifact but may not perform this proof search.

## Evidence and provenance boundary

Use these immutable inputs only at their audited scopes.

| Input | Immutable provenance | Permitted status in this turn |
| --- | --- | --- |
| Response68 | response `23ca5b89b13ae879c546598d7b56888a92fc39f2`; corrected audit tip `fb3db2e6e95bfb3e323d183000744643e420dbfa`; integrated audit `35f23f59157762f8978bbdbd93f05ae4001c1330` | With corrected `H_n=2n*5^(n-1)*4^n+1`, bounded annihilator and rational-hyperplane necessary conditions are `proved-math`; cofinite prime forcing remains `open`. |
| Response69 | response `ed08c419d83b9f46daf7d30894c4b578098544f0`; audit `b98b249e545f9f24767a53f071aeed091062f622` | Cover/boundary/scaling/one-owner and `n=2` claims are `proved-math`; nearest-boundary transport is `rejected`; per-row compensation remains `open`. |
| Response70 | response `7ca41ed3b537d6270ad07de52fdaf44e80b73f7d`; audit `d44a5d4508ded714296ec64b1619446355d5388a` | Coarse divisibility, singleton rigidity, strict-growth, and all-but-one family are `proved-math`; private-to-gcd claims are `rejected`; common-prime is equivalent to LRC. |
| Response72 | response `dc644fd6828a47400e0456663ccb0d1d5c60876e`; integrated audit `db0e33091f93311fd95143a0160e7afd60f9e3e8` | Canonical circuits, the boundary-grid theorem, and the minimal-dimension no-nonempty-proper-closed-subset theorem are `proved-math`; circuit escape is equivalent to LRC and stopped. Artifacts were not executed and remain unaccepted. These results are outside this target. |
| Response74 | response `967cabe9d0a94c2504e36e10473ef5eccef87adc`; integrated audit `94442b430f4dbb9b66ec186e5655d556ee4e477e` | The common-scaling identity and exact family `(c,2c,8c)`, `c>=6`, are `proved-math`; the arbitrary signed-Gram bridge is `rejected`; a primitive-only or scale-invariant replacement remains `open`. |

Do not use Prompt75 output or execute recovered attachments. Use
`proved-lean`, `proved-math`, `computed finite evidence`, `conditional`,
`conjectural`, `rejected`, `external-unformalized`, and `open` literally.
Prior response self-dispositions do not promote claims.

## Excluded routes

Do not use or rename prime forcing; Prompt76 private-demand Hall transport;
Prompt66 packing, radial descent, or signed phase cells; Prompt64 top-two;
Prompt62 owner shifts; Gamma matching; height compression; deletion descent;
first blockers; Response72 circuit escape; common-prime propagation; the
Prompt74 theorem for arbitrary nonprimitive tuples; a zero-frequency identity;
a delta basis; Parseval applied to the unknown good indicator; or the tautology
that some `G_j>0`.

This contract's sole missing lemma is a primitive fixed-degree signed-Gram
gap. It does not share Prompt76's missing aggregate inequality.

## Frozen primitive normalization

Let `n : Nat`, `2 <= n`, `N=n+1`, and let `a : Fin n -> Nat` be positive and
injective. Define `g(a)` as the positive iterated gcd in increasing index
order. Call `a` primitive when `g(a)=1`. Do not confuse tuple primitivity with
pairwise coprimality.

For an arbitrary positive injective tuple `A`, put `d=g(A)` and `a_i=A_i/d`.
Prove that `d>0`, each division is exact, `a` is positive and injective, and
`g(a)=1`. Pairwise gcds, nonunits, repeated residues, and coincident bad sets
remain allowed. Prove the scale lift below; do not make an informal real-to-
integer reduction.

## Frozen modular definitions and strict boundary

For pivot `j`, put

```text
p_j = a_j,
M_j = N*p_j,
rho_M(x) = min(x mod M, M-(x mod M)),
R_j = {r in {0,...,M_j-1} : N does not divide r},
B_i^j = {r in R_j : rho_(M_j)(r*a_i) < p_j}  for i != j,
mu_j(r) = #{i : i != j and r in B_i^j},
Q_j = {r in R_j : mu_j(r)=0},
G_j = |Q_j|.
```

Badness is strict `<p_j`; equality is safe. Retain both cyclic sides, owner
multiplicity, reflection fixed points, nonunits, gcd degeneracies, and
different pivot moduli. Never cancel a nonunit or deduplicate coincident bad
sets.

## Frozen degree-`2n` signed Gram forms

Let `D_n=Fin(2*n)`. For `m : D_n`, `r : Fin M_j`, and
`z : D_n -> Complex`, define

```text
chi_(j,m)(r) = exp(2*pi*I*m.val*r.val/M_j),
P_(j,z)(r) = sum_m z_m*chi_(j,m)(r),
E_j(z) = (1/M_j) * sum_(r in R_j)
           (1-mu_j(r))*|P_(j,z)(r)|^2,
K_j[m,l] = (1/M_j) * sum_(r in R_j)
  (1-mu_j(r))*conjugate(chi_(j,m)(r))*chi_(j,l)(r).
```

Here `1-mu_j(r)` is an integer embedded in the reals. State complex norm and
embedding conventions. Prove `K_j` Hermitian and
`E_j(z)=conjugate(z)^T K_j z`. Modes are literal integers `0,...,2n-1`; retain
aliasing when `M_j<2n`.

For block vector `Z=(z_j)_j`, define

```text
||Z||^2 = sum_j sum_m |z_j(m)|^2,
E_a(Z) = sum_j E_j(z_j).
```

No height-dependent cutoff, hidden positive rescaling, arbitrary block weight,
post-hoc frequency choice, or good-set indicator may enter these definitions.

## Sole unrestricted lemma

```text
PRIMITIVE-DEGREE-2N-SIGNED-GRAM-GAP:

For every n>=2 and every positive injective primitive
a : Fin n -> Nat, there exists a block vector Z such that

  ||Z||^2 = 1  and  E_a(Z) > 0.
```

Equivalently one block `K_j` has a positive eigenvalue, after proving the
block-diagonal equivalence. This is a fixed-dimensional assertion about signed
cover multiplicities, not the definition of an uncovered residue. If the only
proof constructs `Z` from a known `Q_j` residue, uses all `M_j` modes, or
rewrites the lemma as certificate existence, return `STOP`.

The primitive hypothesis is the only permitted change from Prompt74. Use the
accepted common-scaling theorem at its exact scope, independently rederive why
its nonprimitive family does not decide the primitive target, and identify
which part of the scaling-fiber mechanism disappears or survives after gcd
normalization. Do not infer that primitivity proves the gap. A primitive exact
counterexample stops this bridge but does not refute LRC.

## Complete implication chain

Handle `n=1` directly. For `n>=2`, normalize arbitrary positive injective `A`
to primitive `a` and apply the lemma. If every pivot candidate were covered,
then `mu_j(r)>=1`, hence every coefficient `1-mu_j(r)<=0`, forcing
`E_a(Z)<=0`. Thus some `j,r` has `mu_j(r)=0`. Expand this into avoidance of all
strict bad sets and prove pivot safety from `N` not dividing `r`.

For the scale lift prove explicitly that the same integer `r`, at modulus
`N*A_j=d*N*a_j`, gives

```text
t = r/(N*A_j) = (r/(N*a_j))/d,
rho_(N*A_j)(r*A_i) = d*rho_(N*a_j)(r*a_i).
```

Check representatives and strict/equality boundaries. Finish through the
existing `proved-lean` directions

```text
one pivot certificate
  -> PositiveIntegerPivotCertificateConjecture
  -> PositiveIntegerConjecture
  -> Conjecture
  -> unrestricted real LRC.
```

Do not claim this implication proves the frozen lemma.

## Attack A: exact primitive Hermitian separation

Recompute every matrix entry from the strict candidate and bad-set transforms.
Seek an exact primitive-only trace, principal-minor, Schur-complement, minimax,
or signed-energy inequality forcing a positive Rayleigh quotient. The new
input must genuinely use `g(a)=1` across the tuple, such as a proved Bezout
identity coupled to all pivot blocks. Pairwise coprimality may not be assumed.
A common lcm alone supplies no cross-pivot cancellation.

Allowed certificates are exact rational/cyclotomic coordinates, exact minor
signs, or rigorous algebraic isolating intervals. Floating eigenvalues are
falsification diagnostics only. Retain all negative overlap terms and do not
select a pivot from known `G` data.

## Attack B: simultaneous all-pivot additive energy

Pull the forms to `L=lcm_j M_j` only after proving projection fibers and exact
normalization. Seek a sum-of-squares or additive-energy identity whose
primitive Bezout data couples at least two pivot blocks and yields the strict
positive direction. State the first new inequality before its proof.

Preserve literal `2n` modes and signed coefficients. A carrier rewrite,
independent block optimization, arbitrary eigenvalue-dependent weights, or an
expression expanding to `1_Qj` is not progress. Finish at the gap or identify
the first fatal cancellation arrow.

## Mandatory accepted-result rederivation and falsification

Do not execute recovered attachments. The Response74 common-scaling theorem
and exact family are accepted `proved-math` and need not be re-audited as new
claims. Nevertheless, rederive their definitions and the primitive distinction
inside this memo so the proposed replacement is independently intelligible.

1. Starting from the accepted scaling theorem, rederive for
   `(6,12,48)=6*(1,2,8)` that `G=(0,0,36)` and
   `K_1=-I/4`, `K_2=-I/4`, `K_3=0`. Explain exactly why the theorem does not
   decide the primitive target and do not relabel the accepted result as new.
2. `(1,2,8)`: `G=(0,0,6)`. Compute every exact block.
3. `(1,2,3,4,5,7)`: `G=(0,0,0,0,2,2)`; test negative overlaps.
4. `(1,6,11,12,13)`: `G=(0,0,8,8,10)`; two rows are covered.
5. `(4,5,9)`: `G=(2,2,4)` but factorwise Fourier domination fails at every
   pivot; do not revive it.
6. `(1,2,3)`: `G=(2,2,2)` and equality `rho=3` is safe at pivot 3.
7. `(1,3,5)`: classify reflection fixed points without doubling.
8. `(1,3,13)`: coincident owner bad sets retain multiplicity.
9. `(1,3,4,5,7,11)`: test literal modes and aliasing.
10. Test excluded `N`-multiples, `rho=p_j-1`, equality, antipodes, gcd fibers,
    permutations, and common scalings by 2 and 3.

Before a universal proof, perform a deterministic exact search over primitive
strictly increasing tuples for `2<=n<=6`, maximum speed at most 14, ordered by
`n` then lexicographically. Stop at the first tuple for which every exact
block is negative semidefinite. Emit the tuple, every `M_j`, `G_j`, exact
cyclotomic matrix, and exact semidefiniteness certificate. Common scalings are
diagnostics only. Zero failures are only `computed finite evidence`; floating
output remains experimental.

Any code must be separately hashed with deterministic ordering, declared
language/version, exact domain, malformed-input behavior, algebraic-number
representation, interval policy, source/output binding, and a machine-readable
first-failure record. Luna may not infer missing semantics.

## Required response format

Return exactly:

1. **Disposition** — `BRIDGE`, `PIVOT`, or `STOP`.
2. **Status table** — one exact evidence label per claim and dependency.
3. **Frozen target** — normalization, types, matrices, and boundary.
4. **Prompt74 accepted theorem** — exact rederivation and primitive distinction.
5. **Anti-tautology audit** — first genuinely new spectral inequality.
6. **Exact falsification** — fixtures, bounded domain, and first exact failure.
7. **Attack A** — exact Hermitian proof or first fatal arrow.
8. **Attack B** — simultaneous-energy proof or first fatal arrow.
9. **Complete implication chain** — gcd normalization, `n=1`, scale lift,
   pivot safety, and exact Lean directions.
10. **Artifacts and final evidence boundary** — hashes and finite scope only.

`BRIDGE` requires the gap at every quantifier and the full normalization chain.
`PIVOT` requires a proved unrestricted primitive spectral theorem strictly
narrowing the gap without an equally strong existential. Finite success is
never `PIVOT`.

## One-turn budget and STOP rules

Use one major Sol Pro turn: first third exact rederivation/falsification,
second primitive Hermitian separation, final third simultaneous energy.

Return `STOP` if a primitive exact tuple makes every block negative
semidefinite; the proof uses a known good residue, delta basis, all `M_j`
modes, or a height cutoff; primitivity becomes pairwise coprimality; the open
edge becomes certificate existence, Prompt76's inequality, or LRC; common-lcm
rewriting supplies no sign control; a nonunit is cancelled; strict badness is
weakened; owners are deduplicated; or only floating/bounded evidence remains.

A rigorous counterexample or exact first-fatal-arrow memo is a successful
response. Do not edit the tracked repository or claim unrestricted LRC.
