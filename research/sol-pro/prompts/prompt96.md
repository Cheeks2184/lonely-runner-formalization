---
owner: GPT-5.6 Sol High top-level desktop orchestrator
supervising_authority: /root
launch_runtime: original browser-capable desktop Codex session
role: GPT-5.6 Sol / Pro adversarial mathematical synthesis agent
task_id: SOL-P96-ADVERSARIAL-SYNTHESIS-CONTRACT-PREP-299
base_commit: e3055a1acdab1476320a1dc742767cd7e2451206
admission: REVIEW-REQUIRED
launch_dependency: PROMPT92-RECOVERY-298-COMMIT-2e5554c508e5ca05a016994e53ce66af31a4702b
---

# Prompt 96: PROMPT92 COUNTERFAMILY AND SURVIVING-ROUTE SYNTHESIS

You are GPT-5.6 Sol running at Pro effort in a genuinely new ChatGPT
conversation. Act as the mathematical adversarial-synthesis agent. This is a
single focused research turn triggered by a substantive candidate STOP for
Prompt92. It is not permission to begin a fourth unrelated broad search.

If this contract is submitted, Sol High has already integrated and reviewed
the Prompt92 recovery dependency specified below and has launched a reserved
Pro cell. Attack the exact synthesis target now. Do not review admission,
prepare another prompt, browse, edit a repository, or claim unrestricted
Lonely Runner.

This tracked file is presently preparation only. It creates no launch,
response, proof, counterexample, audit, computation, or evidence promotion.
Recovery task 298 is frozen below by exact commit, paths, and hashes, but its
mathematical payload remains response-authored and unverified. This contract's
admission remains `REVIEW-REQUIRED` until independent contract review accepts
its exact integrated blob.

Use one major turn. Independently prove or refute the displayed symbolic
family, determine its exact effect on the Prompt87/Prompt92 aggregate branch,
and compare Prompt94 and Prompt95 only through their frozen operative
statements. A bounded scan, response attachment, or attractive analogy is not
a theorem.

## Research contract

| Field | Frozen value |
| --- | --- |
| Exact target | `P92-ALL-E-COUNTERFAMILY-AND-ROUTE-SEPARATION` below |
| Family scope | every integer `E>=3`, `q=2^E`, `M=7q`, one labelled six-coordinate residue tuple |
| Required synthesis | family theorem -> exact Prompt87/92 killed-arrow map -> exact Prompt94/95 transfer or separation result |
| Frozen boundaries | full grid including zero; strict badness; canonical representatives; repeated labels and nonunits retained |
| Unverified trigger | response-authored Prompt92 family statement recovered by recovery298; no mathematical promotion |
| Surviving routes compared | Prompt94 unit-stratum deficit and Prompt95 max-covered-pivot redundancy, at their exact quantified domains only |
| Stop condition | first failed family identity, incomplete cover cell, invalid route transfer, or reliance on unrecovered material |
| Maximum budget | one major Pro turn; no unrelated search and no repair of Prompt92 |

## Launch dependency and evidence boundary

Unrestricted LRC remains open. The following tracked files are the only
repository inputs. SHA-256 values cover exact committed bytes: the original
route files are frozen at base
`e3055a1acdab1476320a1dc742767cd7e2451206`, and the three Prompt92 recovery
files are frozen at recovery commit
`2e5554c508e5ca05a016994e53ce66af31a4702b`.

```text
research/sol-pro/prompts/prompt87.md
  0a9456c26579ee3e092b8d5e73dae16334226f4ed482d43759a049df8617bbff

research/sol-pro/responses/response87.md
  228e51affc27c39981cdf3f5f086a17d2ddcef2d1c1a528c74a9ca281a566d5c

research/sol-pro/audits/response87-mathematical-audit-270.md
  8beb0edd8e4f854fd536fe2ac7fc765acf01f502032da6fb142c72b734fd93ca

research/sol-pro/prompts/prompt92.md
  654f8bc4de240642483cd83898c6cd272cf0eb3635ab9c08971bdc2d2ac2c3dd

research/sol-pro/launches/prompt92-2026-08-04.md
  fda840e7f434917bf5c0350849628c11ff9cce2644a1642a97967be728a19707

recovery commit
  2e5554c508e5ca05a016994e53ce66af31a4702b

research/sol-pro/responses/response92.md
  b7299f87eabc0d4836ec15cb14fae5f2286389703ca28c5d20dd973d021ec8f6

research/sol-pro/audits/response92-recovery-audit-298.md
  1a5908a8fe719fb6b735ef8d2ff4d67432bfe1a08dad48d44505e37389357b1c

research/sol-pro/artifacts/prompt92/recovery-manifest.txt
  d5b45eee7a5127c98fa223bf1858f29fc81aad2b85138c797fdad4d876bc8579

research/sol-pro/prompts/prompt94.md
  dfb9d28e46091512864edb182524a60819d5ae0e34b4d1c4b9ba05f09e0e3ae8

research/sol-pro/launches/prompt94-2026-08-04.md
  baa90c569761e85700813f4e6f8cd832272b9afff0fe0f4ced510c2378d89466

research/sol-pro/prompts/prompt95.md
  3fab778e3e11adb920304f8c2537737da24fc31c16c10fb3208b68617d9643fb

research/sol-pro/launches/prompt95-2026-08-04.md
  85589e81b09938b03f6b76fabecbb54c57aae685110594868d1a79d83a88bbe4
```

Use them only at these scopes:

- Prompt87's tracked response and audit establish their accepted historical
  scopes. They record the aggregate dyadic product-divisibility statement as
  an open edge; they do not prove it.
- Prompt92's tracked contract fixes the unrestricted aggregate lemma and its
  conditional C2 chain. Its launch record proves only launch provenance.
- Recovery298 normalized the Prompt92 response from CRLF to LF only. The raw
  decoded response SHA-256 is
  `3af0da9f6dba3f1a191d8944cddbfa12cdd4348ab6df3e276c8b7c55ae3560e3`;
  the tracked normalized response SHA-256 is the value above. The recovery
  audit preserves the response's `STOP` only as response-authored/unverified.
  Its quarantined Python, JSON, manifest, and ZIP attachments were not
  executed, imported, parsed as evidence, opened, or extracted.
- Prompt94 and Prompt95 are active research routes. Their tracked prompts fix
  their operative statements; their launch records prove only launch
  provenance. No response result is assumed.
- Prompt93 is a separate prelaunch common-clock rational-score/`Psi` route
  reported stopped. No tracked Prompt93 input exists at this base, so no
  Prompt93 theorem, counterexample, score, or evidence label is a premise.

Before launch, verify all of:

```text
the integrated contract commit descends from recovery commit
2e5554c508e5ca05a016994e53ce66af31a4702b
the three recovery paths and SHA-256 values above match exact committed bytes
the full raw-response SHA-256 matches the recovery manifest and audit
every mathematical claim remains response-authored, unverified input
```

Any contract-byte change requires a new hash and independent contract review.
The correct current lifecycle status is `REVIEW-REQUIRED`, not
`LAUNCH-CANDIDATE` or `LUNA-READY`.

Use these evidence labels exactly:

```text
input-accepted
proved-lean
proved-math-qualified
proved-math-response
computed finite evidence
refuted-exact
conditional
conjectural
open
not-run
unverified response input
out-of-scope
```

No response-authored statement may be promoted inside this turn.

## Frozen Prompt92 definitions at dimension six

Put

```text
n = 6,
N = n+1 = 7.
```

For every `E>=3`, define

```text
q = 2^E,
M = N*q = 7q,
C = M/4 = 7q/4,
H = M/2 = 7q/2,

v = (v_0,v_1,v_2,v_3,v_4,v_5)
  = (1,1,C-1,C+1,H-1,H).
```

Because `E>=3`, all six entries are integers in the canonical representative
domain `{0,...,M-1}`. The two coordinates equal to `1` are distinct labels.
They may not be merged. Positivity and injectivity are not premises of the
Prompt92 residue lemma.

For canonical `r in {0,...,M-1}`, put

```text
rho_M(x) = min(x mod M, M-(x mod M)),
B_i = {r : rho_M(r*v_i) < q}.
```

The grid is the complete `Fin M`. It includes `r=0`, multiples of `7`,
nonunits, antipodes, collisions, and every cyclic side. Equality `rho_M=q`
is safe.

For each deletion label `k`, define

```text
Delta_k(v) = gcd(M, {v_i : i!=k}),
```

where the gcd contains the modulus and all five surviving labelled
coordinates. Product divisibility uses the natural product of the six
canonical representatives.

## Unverified response-authored family statement

Recovery298 preserves the following response-authored statement. It is frozen
here as `unverified response input`, not as a premise or result:

```text
For every E>=3, the tuple v above satisfies

  (1) Delta_k(v)=1 for every k;
  (2) union_(i=0,...,5) B_i = Fin M;
  (3) nu_2(product_i v_i)=E-1;

therefore q does not divide product_i v_i.
```

Independently rederive every clause. Do not cite the Prompt92 response as its
proof, and do not treat agreement with a recovered attachment as independent
verification.

## Frozen expected strict-cover decision table

The following table is an exact falsification target, not accepted evidence.
Prove or reject every row. The cases must be shown disjoint and exhaustive for
canonical `r`.

| Canonical residue case | Proposed covering owner | Required exact calculation |
| --- | ---: | --- |
| `r` even | `v_5=H` | `rH=0 mod M`, hence `rho_M(rH)=0<q` |
| `r` odd and `rho_M(r)<q` | `v_0=1` | endpoint arcs are strictly bad |
| `r` odd and `abs(r-H)<q` | `v_4=H-1` | `r(H-1)=H-r mod M` and distance is `abs(r-H)` |
| `r` odd, `q<r<H-q`, `r=1 mod 4` | `v_2=C-1` | `rC=C mod M`, so distance is `abs(C-r)<q` |
| `r` odd, `q<r<H-q`, `r=3 mod 4` | `v_3=C+1` | `rC=3C mod M`, so distance is `abs(C-r)<q` |
| `r` odd, `H+q<r<M-q`, `r=3 mod 4` | `v_2=C-1` | reflect by `s=M-r`, then use the lower `s=1 mod 4` row |
| `r` odd, `H+q<r<M-q`, `r=1 mod 4` | `v_3=C+1` | reflect by `s=M-r`, then use the lower `s=3 mod 4` row |

Do not infer coverage merely from interval lengths. Prove the congruences,
the nearest cyclic representative, the strict inequality, and exhaustion.
The four interval endpoints `q`, `H-q`, `H+q`, and `M-q` are even and must be
handled by the even row, not silently assigned to an open odd interval.

The response also claims a stronger fixed-grid anti-redundancy fact. After
deleting one duplicate `1`, retain the five labelled owners

```text
w=(1,C-1,C+1,H-1,H).
```

The claim is that these five sets still cover `Fin M` and form an
inclusion-minimal labelled cover, with these private residues in owner order:

```text
owner 1:   r=1
owner C-1: r=q+1
owner C+1: r=q+3
owner H-1: r=H-1
owner H:   r=q
```

At each displayed residue the named owner must be strictly bad and all other
four owners safe. Independently prove or reject every badness vector. This is
an `unverified response input` and a fixed-grid statement only.

## Sole exact synthesis target

Prove or refute exactly:

```text
P92-ALL-E-COUNTERFAMILY-AND-ROUTE-SEPARATION:

A. For every E>=3, the displayed labelled tuple is canonical, satisfies all
   six deletion-gcd premises, strictly covers every r mod 7*2^E, and has
   exact product valuation E-1.

B. At E=E_6 from Prompt92, this tuple is an exact counterexample to
   AGGREGATE-DYADIC-C2-COVER-DIVISIBILITY in dimension six.

C. The exact killed and surviving arrows in Prompt87 and Prompt92 are those
   proved in the route-impact audit below; no LRC conclusion follows.

D. At the frozen operative statements, each Prompt94/Prompt95 relation is
   classified by an exact instantiation, an unrestricted transfer theorem,
   or a proved no-direct-transfer/independence map.
```

Part D is not permission to make a broad new conjecture. The family theorem
and its exact logical consequences are the center of the turn.

## Elementary family obligations

Before route synthesis, prove all of the following.

1. `E>=3` implies `8|q`, `4|M`, and `2|H`.
2. `0<=v_i<M` for every labelled coordinate. State why `C-1>0`.
3. Every deletion leaves one of the two labelled `1` coordinates, so every
   `Delta_k(v)` is exactly one. Do not substitute total gcd one.
4. `C-1`, `C+1`, and `H-1` are odd, while

   ```text
   nu_2(H)=E-1.
   ```

   Therefore the exact labelled product has valuation `E-1`.
5. Prove `q does not divide product_i v_i`; do not replace it with
   coordinatewise nondivisibility.
6. Prove the seven decision-table rows above, including reflection and
   exhaustion, to establish the full strict cover.

The case `E=2` is outside the frozen family: the parity calculation for
`C+/-1` changes. Do not call the theorem "all positive E."

## Mandatory strict-boundary and label controls

Reproduce all controls symbolically and at the literal base `E=3`.

For `E=3`:

```text
q=8,
M=56,
C=14,
H=28,
v=(1,1,13,15,27,28).
```

Required checks:

1. All six deletion gcds are `1`.
2. The exact product valuation is `2=E-1`.
3. Every residue `0,...,55` is covered under strict `<8` badness.
4. `r=0` is retained and has zero product for every owner.
5. At `r=8` and `r=48`, owners `0,1,2,3,4` have distance exactly `8` and
   are safe; owner `5` has distance zero and supplies coverage.
6. A deliberate `<=q` implementation must disagree with the owner-membership
   vectors at `r=8` and `r=48`, even though the cover union remains full.
7. `B_0=B_1` as residue sets, but the owner labels and the two product factors
   remain distinct.
8. The nonunit coordinate `H=M/2` and all its zero multiplication fibers are
   retained.
9. Reflection `r -> M-r` preserves every labelled bad predicate and swaps the
   lower and upper odd annuli without losing endpoints.

Also check `E=4`, where

```text
q=16,
M=112,
v=(1,1,27,29,55,56),
nu_2(product_i v_i)=3,
```

only as a boundary/mechanism control. Two finite cases are not a universal
proof.

Malformed controls must reject noninteger `E`, `E<3`, a changed modulus,
wrong tuple length, a coordinate outside the canonical range, a deleted owner
label, weak badness, a total-gcd replacement, or a grid omitting zero.

## Exact Prompt87 and Prompt92 impact audit

If Part A is proved, derive the impact rather than asserting it.

Prompt92 defines `E_n>=3`, `q_n=2^E_n`, and `M_n=(n+1)q_n`. Instantiate the
family with `n=6` and `E=E_6`. The response claims `E_6=117`; independently
derive this from Prompt92's exact natural-number definitions of `T_6` and
`E_6`, including

```text
C_6=21,
A_6=21^30=4640650289117164100520051333566036654601,
T_6=99465241107620972662038137293510731,
2^116 <= T_6 < 2^117.
```

Prove that the family definitions then coincide exactly with
`q=q_6=2^117` and `M=M_6=7*2^117`; then identify these consequences:

- `AGGREGATE-DYADIC-C2-COVER-DIVISIBILITY` is `refuted-exact` because every
  premise holds and `q_6` does not divide the labelled product.
- Its exact contrapositive is refuted by the same tuple: product
  nondivisibility holds but no common safe fixed-grid residue exists.
- The conditional C2 chain fails exactly where it applies the aggregate
  lemma. It cannot obtain `q_n|product_i v_i` or lift that divisibility to the
  speed product.
- Prompt87's proposed aggregate-dyadic surviving edge and the corresponding
  aggregate branch of C2 are killed at their frozen quantifiers.

Here `refuted-exact` is the response's mathematical disposition if the proof
closes. At repository level it remains `proved-math-response` until recovery
and independent audit accept it; this turn cannot promote status.

Do not claim that the tuple refutes:

- unrestricted LRC or the accepted positive-integer/real equivalence;
- the elementary definitions or inequalities for `T_n,E_n,q_n,M_n`;
- deletion-gcd lifting, counterexamplehood-to-cover, modular product
  congruence, or pivot-witness lifting when their premises hold;
- the external MSS strict-product theorem;
- Prompt87's separate distinct-prime/obstruction route; or
- any theorem whose premise requires positive injective speeds rather than an
  arbitrary labelled residue tuple.

The earlier Prompt87 family
`(1,4,5q/2-1,5q/2)` satisfied aggregate product divisibility and therefore did
not refute the aggregate edge. Keep it separate from the new six-coordinate
family. Do not retroactively change the accepted scope of its audit.

## Frozen Prompt94 comparison

Prompt94's operative target is exactly:

```text
For every n>=2 and every positive injective integer speed tuple a,
OmegaUnit(a)>0,
```

where each pivot uses its own modulus `M_j=(n+1)a_j`, its candidate row
excludes multiples of `n+1`, and its uncovered numerator must be a unit modulo
the entire pivot modulus.

The Prompt92 family is not by itself a Prompt94 tuple-level counterexample:

- it repeats the speed value `1`;
- its six bad owners live on one externally fixed full modulus `7q`;
- Prompt94 has six pivot rows, each with only the five nonpivot owners;
- Prompt94 excludes multiples of `7` from each candidate row; and
- its conclusion concerns a unit certificate, not product divisibility.

These are type and quantifier checks, not a proof that Prompt94 is true.

The family does refute the following strictly stronger *fixed-grid* statement:

```text
deletion gcds + q not dividing the product
  -> exists a unit r mod M safe for every fixed-grid owner.
```

Indeed, a proved full cover leaves no safe residue of any gcd stratum. Record
this only as `FIXED-GRID-UNIT-STRENGTHENING-REFUTED`; do not identify it with
Prompt94's all-pivot `OmegaUnit` theorem.

To classify `FALSIFIES-P94`, supply a positive injective tuple and compute
every exact row deficit to prove `OmegaUnit=0`. To classify
`CLOSES-P94-EDGE`, prove one unrestricted lemma that discharges Prompt94's
first open positivity arrow for every allowed tuple. Otherwise return
`NO-DIRECT-TRANSFER` and prove precisely why the supplied family data do not
instantiate or imply the operative statement. Do not call absence of a map a
model-theoretic independence theorem.

## Frozen Prompt95 comparison

Prompt95's operative target is exactly
`MAX-COVERED-PIVOT-INTERNAL-COVER-REDUNDANCY` for every positive injective
integer speed tuple. It uses pivot-dependent moduli, candidate rows excluding
multiples of `N`, the set of fully covered pivots `F(a)`, the unique
maximum-speed covered pivot `j_star`, and labelled internal complete covers.

The Prompt92 family alone does not instantiate Prompt95:

- repeated `1` violates injectivity;
- the fixed-grid tuple supplies six owners, not a pivot plus its five owners;
- inserting a new pivot speed `q` would change the dimension and hence change
  `N` from `7` to `8`;
- no `F(a)`, `j_star`, or internal owner cover follows from one fixed-grid
  union; and
- full `Fin M` coverage is not Prompt95's candidate-row coverage statement.

If the five-owner private-residue table above is proved, it refutes the broad
fixed-grid claim that every complete cover has a redundant owner. Record this
only as `FIXED-GRID-REDUNDANCY-STRENGTHENING-REFUTED`. Prompt95 selects an
internal cover at the unique fastest covered pivot of one injective speed
tuple; the fixed-grid minimal cover has none of that structure.

To classify `FALSIFIES-P95`, provide one exact positive injective speed tuple,
compute all pivot rows and `F(a)`, identify `j_star`, and exhibit an internal
complete cover with no redundant owner. To classify `CLOSES-P95-EDGE`, prove
an unrestricted lemma that discharges the exact redundancy theorem's first
open arrow. Otherwise return `NO-DIRECT-TRANSFER` with the exact domain and
arity obstruction. Do not convert the Prompt92 decision table into an
unstated cross-pivot map.

## Anti-equivalence and anti-repair gates

Audit every attempted implication against this diagram:

```text
unverified Prompt92 response family
        |
        | independent symbolic rederivation required
        v
all-E family theorem at E>=3
        |
        +--> instantiate E=E_6 --> refute Prompt92 aggregate lemma
        |                              |
        |                              +--> block only aggregate C2 branch
        |
        +--> Prompt94 comparison -- no arrow without valid injective
        |                           pivot-row instantiation or unrestricted lemma
        |
        +--> Prompt95 comparison -- no arrow without valid injective
                                    all-pivot cover instantiation or unrestricted lemma
```

The following are forbidden:

1. Repairing Prompt92 by changing dimension, exponent, grid, strictness,
   deletion gcd, repetitions, product conclusion, or quantified tuple domain.
2. Adding a counterexample-origin, injectivity, height, unit, primitive, or
   selected-pivot premise to salvage aggregate divisibility.
3. Treating the family as a positive-integer LRC counterexample.
4. Inferring Prompt94 failure from product nondivisibility or Prompt95 failure
   from one fixed-grid full cover.
5. Calling a family-specific identity an unrestricted P94/P95 lemma.
6. Reviving the stopped Prompt93 `Psi` score or importing a common-clock
   denominator.
7. Using Prompt91 Fourier/Fejer, Prompt89 junction, Prompt90 uncrossing,
   Prompt92 repair, packing, or another open route as a hidden premise.
8. Offering a bounded scan, numerical plot, or response-authored attachment
   as the symbolic proof.

## Adversarial attack lanes

### Lane A - algebra and deletion gcd

Prove canonicality, every deletion gcd, and the exact product valuation in a
form valid for all `E>=3`. State the parity of every factor. A valuation lower
bound is insufficient; the result must be exactly `E-1`.

### Lane B - strict full-cover table

Prove the frozen seven-row table from congruences modulo `M`. For each row,
identify the actual signed representative in `(-M/2,M/2]`, prove it has
absolute value `<q`, and treat reflection, equality, zero, and interval
endpoints. If one row fails, return the least `E,r` failure under the frozen
order.

### Lane C - killed-arrow synthesis

Instantiate the all-E theorem at the definitionally selected `E_6` and trace
the Prompt92 proof-obligation graph arrow by arrow. Distinguish a false lemma,
a blocked conditional chain, an external premise, and an unaffected accepted
reduction. Give one canonical killed/surviving table.

### Lane D - surviving-route adversary

First audit the fixed-grid unit and redundancy strengthenings stated above.
Then attempt only exact type-correct transfers into Prompt94 and Prompt95.
Either give a fully verified counterexample at their own domains, prove one
unrestricted lemma closing their exact first open edge, or prove the supplied
family gives no direct transfer and leave both routes open. A new broad search
is out of scope.

## Optional computation boundary

No code attachment is required. Literal checks of `E=3,4` may be performed
ephemerally, but the symbolic proof must stand without them.

If code or output is attached, provide separate source, manifest, and result
files; hash every exact file with SHA-256; state language/version, deterministic
order, domain, exit status, completion marker, and first failure. Label all
such material `unverified response input` until recovery and independent
replay. Missing, stale, mismatched, partial, or chat-only artifacts are
`not-run`, never computed finite evidence.

## Exact disposition semantics

Return one of:

- `COUNTERFAMILY` only if Parts A-C are proved completely and Part D gives an
  exact frozen-route classification without an unsupported transfer.
- `TRANSFER` only if Parts A-C are proved and one new unrestricted lemma
  genuinely closes the first open edge of Prompt94 or Prompt95.
- `INDEPENDENT` only if Parts A-C are proved, neither exact route is refuted,
  and the no-direct-transfer map is proved at the supplied frozen data. This
  means route separation at the current inputs, not absolute logical
  independence.
- `STOP` if the symbolic family fails, a mandatory boundary is inconsistent,
  recovery identity is missing from the launched prompt, or the synthesis
  reaches an unsupported arrow. Give the exact first failure and preserve all
  unaffected routes as open.

No disposition promotes the response. `COUNTERFAMILY`, `TRANSFER`, or
`INDEPENDENT` remains `proved-math-response` pending recovery and independent
audit.

## Required response schema

Return exactly these sections.

1. **Disposition** - `COUNTERFAMILY`, `TRANSFER`, `INDEPENDENT`, or `STOP`.
2. **Recovery and evidence boundary** - exact frozen inputs and labels.
3. **Definitions** - `E,q,M,C,H,v,rho,B_i,Delta_k`, labels, product, and orders.
4. **Canonicality and deletion gcds** - all six deletion checks.
5. **Exact product valuation** - factor-by-factor proof of `E-1`.
6. **Strict full-cover decision table** - seven cases, exhaustion, and witnesses.
7. **Boundary controls** - `E=3`, `E=4`, equality, zero, reflection, repeats, and nonunits.
8. **Prompt92 instantiation** - exact substitution `E=E_6`.
9. **Killed-arrow table** - Prompt87/92 claim, dependency, status, and first failed arrow.
10. **Surviving-arrow table** - elementary, external, accepted, and separate open arrows.
11. **Fixed-grid strengthening audit** - unit-safe and five-owner redundancy consequences.
12. **Prompt94 comparison** - exact counterexample, unrestricted transfer, or `NO-DIRECT-TRANSFER`.
13. **Prompt95 comparison** - exact counterexample, unrestricted transfer, or `NO-DIRECT-TRANSFER`.
14. **Anti-equivalence audit** - domains, arities, moduli, candidates, labels, and conclusions.
15. **Dependency graph** - family proof through aggregate failure and both route comparisons.
16. **Optional artifact ledger** - hashes and quarantine labels, or `not-run`.
17. **Proof-obligation ledger** - claim, dependencies, evidence class, falsifier, and downstream owner.
18. **Final evidence boundary** - separate accepted inputs, response reasoning, computation, conditional claims, refuted exact claims, and open routes.

## One-turn budget and fail-closed rules

Use the first quarter for algebra, deletion gcds, and literal boundary checks;
the second quarter for the symbolic cover table; the third quarter for the
Prompt87/92 arrow map; and the final quarter for the exact Prompt94/95
comparison, dependency graph, and evidence ledger.

Return `STOP` immediately on:

- any noncanonical family coordinate or deletion gcd other than one;
- any uncovered canonical residue for one `E>=3`;
- failure of the exact valuation `E-1`;
- a weak/strict swap, omitted zero, lost cyclic side, or deduplicated `1`;
- use of `E=2` as though it were inside the family;
- failure to instantiate Prompt92 at its actual `E_6`;
- a claim that the family itself is an injective Prompt94/95 speed tuple;
- a Prompt94 or Prompt95 conclusion without its own frozen premises;
- an invented repair of Prompt92 after its exact failure;
- any mismatch in the full recovery commit/path/hash identities;
- treatment of an attachment or bounded replay as a universal theorem; or
- any claim that unrestricted LRC is proved or refuted.

A proved family and exact STOP map are valuable even if neither surviving
route advances. Preserve Prompt94 and Prompt95 as `open` unless their exact
operative statements are separately proved or refuted.
