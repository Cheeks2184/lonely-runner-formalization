---
owner: GPT-5.6 Sol High top-level desktop orchestrator
supervising_authority: /root
launch_runtime: original browser-capable desktop Codex session
role: GPT-5.6 Sol / Pro adversarial mathematical synthesis agent
task_id: SOL-P96-ADVERSARIAL-SYNTHESIS-CONTRACT-PREP-299
base_commit: f64092fecea12246a05ee7dfe6748df7f2454c41
admission: REVIEW-REQUIRED
accepted_dependency: P92-AUDIT-301-COMMIT-15dfcd14bd2e4230b042cf5f8c4c717a006b6913
---

# Prompt 96: PROMPT92 COUNTERFAMILY AND SURVIVING-ROUTE SYNTHESIS

You are GPT-5.6 Sol running at Pro effort in a genuinely new ChatGPT
conversation. Act as the mathematical adversarial-synthesis agent. This is a
single focused research turn triggered by a substantive candidate STOP for
Prompt92. It is not permission to begin a fourth unrelated broad search.

If this contract is submitted, Sol High has already integrated and reviewed
the Prompt92 recovery dependency specified below and has launched a reserved
Pro cell. Attack the exact synthesis target now. Do not review admission,
prepare another prompt, browse, edit a repository, or claim unrestricted
Lonely Runner.

This tracked file is presently preparation only. It creates no launch,
response, proof, counterexample, audit, computation, or evidence promotion.
Independent audit301 is frozen below and accepts the symbolic Prompt92 family
and its exact route refutation as `proved-math-qualified`. Response-authored
attachments and scans remain quarantined and unverified. This contract's
admission remains `REVIEW-REQUIRED` until independent contract review accepts
its exact integrated blob.

Use one major turn. Concisely replicate the accepted symbolic family as a
deliberate independence gate, then spend most of the turn on the derived
injective speed tuple and its exact effect on Prompt94 and Prompt95. Known
replication alone is not a synthesis result. A bounded scan, response
attachment, or attractive analogy is not a theorem.

## Research contract

| Field | Frozen value |
| --- | --- |
| Exact target | `AUDITED-P92-FAMILY-TO-INJECTIVE-P94-P95-SYNTHESIS` below |
| Accepted family scope | every integer `E>=3`, `q=2^E`, `M=7q`, one labelled six-coordinate residue tuple |
| New synthesis object | injective `a_E=(1,q,7q/4-1,7q/4+1,7q/2-1,7q/2)` |
| Required synthesis | concise independent replication -> exact `q`-pivot transfer -> Prompt94/Prompt95 counterexample, transfer, pivot, or separation |
| Frozen boundaries | full grid including zero; strict badness; canonical representatives; repeated labels and nonunits retained |
| Accepted trigger | audit301 family/cover/valuation/`E_6`/Prompt87+92 refutation at `proved-math-qualified` |
| Surviving routes compared | Prompt94 unit-stratum deficit and Prompt95 max-covered-pivot redundancy, at their exact quantified domains only |
| Stop condition | mismatch with accepted audit, invalid `q`-row transfer, incomplete `F`/`j_star` analysis, or unsupported route claim |
| Maximum budget | one major Pro turn; at most one fifth on replication and a strict majority on the derived injective tuple |

## Launch dependency and evidence boundary

Unrestricted LRC remains open. The following tracked files are the only
repository inputs. SHA-256 values cover exact committed bytes at authoritative
base `f64092fecea12246a05ee7dfe6748df7f2454c41` or the explicitly named audit
commit.

```text
research/sol-pro/prompts/prompt87.md
  0a9456c26579ee3e092b8d5e73dae16334226f4ed482d43759a049df8617bbff

research/sol-pro/responses/response87.md
  228e51affc27c39981cdf3f5f086a17d2ddcef2d1c1a528c74a9ca281a566d5c

research/sol-pro/audits/response87-mathematical-audit-270.md
  8beb0edd8e4f854fd536fe2ac7fc765acf01f502032da6fb142c72b734fd93ca

research/sol-pro/prompts/prompt92.md
  654f8bc4de240642483cd83898c6cd272cf0eb3635ab9c08971bdc2d2ac2c3dd

research/sol-pro/launches/prompt92-2026-08-04.md
  fda840e7f434917bf5c0350849628c11ff9cce2644a1642a97967be728a19707

recovery commit
  6fb384d24ad4d8bc04013d8a7117d8d9d526f0b5

research/sol-pro/responses/response92.md
  b7299f87eabc0d4836ec15cb14fae5f2286389703ca28c5d20dd973d021ec8f6

research/sol-pro/audits/response92-recovery-audit-298.md
  1a5908a8fe719fb6b735ef8d2ff4d67432bfe1a08dad48d44505e37389357b1c

research/sol-pro/artifacts/prompt92/recovery-manifest.txt
  d5b45eee7a5127c98fa223bf1858f29fc81aad2b85138c797fdad4d876bc8579

accepted mathematical audit commit
  15dfcd14bd2e4230b042cf5f8c4c717a006b6913

research/sol-pro/audits/response92-symbolic-counterfamily-mathematical-audit-301.md
  21c738a00bb277f873e8c98762d95b1cd9d8d889a0f3042512e59c08ce6fc69f

research/sol-pro/prompts/prompt94.md
  dfb9d28e46091512864edb182524a60819d5ae0e34b4d1c4b9ba05f09e0e3ae8

research/sol-pro/launches/prompt94-2026-08-04.md
  baa90c569761e85700813f4e6f8cd832272b9afff0fe0f4ced510c2378d89466

research/sol-pro/prompts/prompt95.md
  3fab778e3e11adb920304f8c2537737da24fc31c16c10fb3208b68617d9643fb

research/sol-pro/launches/prompt95-2026-08-04.md
  85589e81b09938b03f6b76fabecbb54c57aae685110594868d1a79d83a88bbe4
```

Use them only at these scopes:

- Prompt87's tracked response and audit establish their accepted historical
  scopes. They record the aggregate dyadic product-divisibility statement as
  an open edge; they do not prove it.
- Prompt92's tracked contract fixes the unrestricted aggregate lemma and its
  conditional C2 chain. Its launch record proves only launch provenance.
- Recovery298 normalized the Prompt92 response from CRLF to LF only. The raw
  decoded response SHA-256 is
  `3af0da9f6dba3f1a191d8944cddbfa12cdd4348ab6df3e276c8b7c55ae3560e3`;
  the tracked normalized response SHA-256 is the value above. The recovery
  audit preserves the response's `STOP` only as response-authored/unverified.
  Its quarantined Python, JSON, manifest, and ZIP attachments were not
  executed, imported, parsed as evidence, opened, or extracted.
- Audit301 independently proves, at `proved-math-qualified`, the all-`E>=3`
  canonical family, every deletion gcd, the strict full cover, exact product
  valuation `E-1`, `E_6=117`, and the exact Prompt87/Prompt92 aggregate-bridge
  refutations. Those are accepted inputs. Audit301 expressly does not promote
  the quarantined scans, packaged fixtures, claimed finite totals,
  inclusion-minimal subcover claim, private-residue claim, or generic fiber
  formulas.
- Prompt94 and Prompt95 are active research routes. Their tracked prompts fix
  their operative statements; their launch records prove only launch
  provenance. No response result is assumed.
- Prompt93 is a separate prelaunch common-clock rational-score/`Psi` route
  reported stopped. No tracked Prompt93 input exists at this base, so no
  Prompt93 theorem, counterexample, score, or evidence label is a premise.

Before launch, verify all of:

```text
the integrated contract commit descends from audit301 commit
15dfcd14bd2e4230b042cf5f8c4c717a006b6913
the recovery and audit paths and SHA-256 values above match exact committed bytes
the full raw-response SHA-256 matches the recovery manifest and audit
accepted audit301 claims retain `proved-math-qualified`
all response attachments and unaudited response claims remain quarantined/unverified
```

Any contract-byte change requires a new hash and independent contract review.
The correct current lifecycle status is `REVIEW-REQUIRED`, not
`LAUNCH-CANDIDATE` or `LUNA-READY`.

Use these evidence labels exactly:

```text
input-accepted
proved-lean
proved-math-qualified
proved-math-response
computed finite evidence
refuted-exact
conditional
conjectural
open
not-run
unverified response input
out-of-scope
```

No response-authored statement may be promoted inside this turn.

## Frozen Prompt92 definitions at dimension six

Put

```text
n = 6,
N = n+1 = 7.
```

For every `E>=3`, define

```text
q = 2^E,
M = N*q = 7q,
C = M/4 = 7q/4,
H = M/2 = 7q/2,

v = (v_0,v_1,v_2,v_3,v_4,v_5)
  = (1,1,C-1,C+1,H-1,H).
```

Because `E>=3`, all six entries are integers in the canonical representative
domain `{0,...,M-1}`. The two coordinates equal to `1` are distinct labels.
They may not be merged. Positivity and injectivity are not premises of the
Prompt92 residue lemma.

For canonical `r in {0,...,M-1}`, put

```text
rho_M(x) = min(x mod M, M-(x mod M)),
B_i = {r : rho_M(r*v_i) < q}.
```

The grid is the complete `Fin M`. It includes `r=0`, multiples of `7`,
nonunits, antipodes, collisions, and every cyclic side. Equality `rho_M=q`
is safe.

For each deletion label `k`, define

```text
Delta_k(v) = gcd(M, {v_i : i!=k}),
```

where the gcd contains the modulus and all five surviving labelled
coordinates. Product divisibility uses the natural product of the six
canonical representatives.

## Accepted audit301 family and independent replication gate

Audit301 proves the following statement as `proved-math-qualified`:

```text
For every E>=3, the tuple v above satisfies

  (1) Delta_k(v)=1 for every k;
  (2) union_(i=0,...,5) B_i = Fin M;
  (3) nu_2(product_i v_i)=E-1;

therefore q does not divide product_i v_i.
```

Replicate every clause directly from the displayed definitions, without
copying audit301's proof or consulting quarantined artifacts. State that this
replication is a deliberate independence check against an accepted result.
Agreement does not create a new theorem or evidence promotion; mismatch is
`STOP` and must identify the first exact discrepancy.

## Frozen expected strict-cover decision table

The following table records the accepted cover mechanism and is the mandatory
concise replication gate. Reprove every row independently. The cases must be
shown disjoint and exhaustive for canonical `r`.

| Canonical residue case | Proposed covering owner | Required exact calculation |
| --- | ---: | --- |
| `r` even | `v_5=H` | `rH=0 mod M`, hence `rho_M(rH)=0<q` |
| `r` odd and `rho_M(r)<q` | `v_0=1` | endpoint arcs are strictly bad |
| `r` odd and `abs(r-H)<q` | `v_4=H-1` | `r(H-1)=H-r mod M` and distance is `abs(r-H)` |
| `r` odd, `q<r<H-q`, `r=1 mod 4` | `v_2=C-1` | `rC=C mod M`, so distance is `abs(C-r)<q` |
| `r` odd, `q<r<H-q`, `r=3 mod 4` | `v_3=C+1` | `rC=3C mod M`, so distance is `abs(C-r)<q` |
| `r` odd, `H+q<r<M-q`, `r=3 mod 4` | `v_2=C-1` | reflect by `s=M-r`, then use the lower `s=1 mod 4` row |
| `r` odd, `H+q<r<M-q`, `r=1 mod 4` | `v_3=C+1` | reflect by `s=M-r`, then use the lower `s=3 mod 4` row |

Do not infer coverage merely from interval lengths. Prove the congruences,
the nearest cyclic representative, the strict inequality, and exhaustion.
The four interval endpoints `q`, `H-q`, `H+q`, and `M-q` are even and must be
handled by the even row, not silently assigned to an open odd interval.

The response also claims a stronger fixed-grid anti-redundancy fact. Audit301
did not accept its inclusion-minimality or private-residue claims. After
deleting one duplicate `1`, retain the five labelled owners

```text
w=(1,C-1,C+1,H-1,H).
```

That these five sets still cover `Fin M` follows from the accepted cover table:
the duplicate labelled `1` sets coincide and only one is needed. The separate
response-authored claim is that the five-owner cover is inclusion-minimal,
with these private residues in owner order:

```text
owner 1:   r=1
owner C-1: r=q+1
owner C+1: r=q+3
owner H-1: r=H-1
owner H:   r=q
```

At each displayed residue the named owner must be strictly bad and all other
four owners safe. Independently prove or reject every badness vector. The
cover is accepted; minimality/private residues remain `unverified response
input` until proved in this turn or separately audited. Everything here is a
fixed-grid statement only.

## Sole exact synthesis target

Prove or refute exactly:

```text
AUDITED-P92-FAMILY-TO-INJECTIVE-P94-P95-SYNTHESIS:

A. Independently replicate, concisely, audit301's all-E canonicality,
   deletion-gcd, strict-cover, valuation, E_6, and exact Prompt87/92 impact.
   This is a gate, not the new synthesis result.

B. For every E>=3, prove that

     a_E=(1,q,C-1,C+1,H-1,H)

   is a positive injective six-speed tuple and that its pivot-q row has
   modulus 7q, nonpivot owners w, a complete strict candidate cover, and
   DefUnit_q(a_E)=0.

C. Determine the exact Prompt94 and Prompt95 consequences of a_E, including
   all Prompt95 F(a_E) and j_star premises used. Do not infer OmegaUnit=0 from
   one zero row deficit.

D. Return exactly one mutually exclusive disposition under the rules below:
   STOP, TRANSFER, COUNTEREXAMPLE, PIVOT, or SEPARATION.
```

Parts B-C are the center of the turn. They are not permission to make a broad
new conjecture or search unrelated tuples.

## Concise independent replication gate

Use no more than one fifth of the turn to independently reproduce:

1. `E>=3` implies `8|q`, `4|M`, and `2|H`.
2. `0<=v_i<M` for every labelled coordinate. State why `C-1>0`.
3. Every deletion leaves one of the two labelled `1` coordinates, so every
   `Delta_k(v)` is exactly one. Do not substitute total gcd one.
4. `C-1`, `C+1`, and `H-1` are odd, while

   ```text
   nu_2(H)=E-1.
   ```

   Therefore the exact labelled product has valuation `E-1`.
5. `q does not divide product_i v_i`; do not replace it with
   coordinatewise nondivisibility.
6. The seven decision-table rows above, including reflection and
   exhaustion, to establish the full strict cover.
7. The exact `E_6=117` arithmetic and Prompt87/Prompt92 killed-arrow map.

This is deliberate independent replication of `proved-math-qualified` input.
It is not `PIVOT`, `TRANSFER`, `COUNTEREXAMPLE`, or `SEPARATION` by itself.

The case `E=2` is outside the frozen family: the parity calculation for
`C+/-1` changes. Do not call the theorem "all positive E."

## Mandatory strict-boundary and label controls

Reproduce all controls symbolically and at the literal base `E=3`.

For `E=3`:

```text
q=8,
M=56,
C=14,
H=28,
v=(1,1,13,15,27,28).
```

Required checks:

1. All six deletion gcds are `1`.
2. The exact product valuation is `2=E-1`.
3. Every residue `0,...,55` is covered under strict `<8` badness.
4. `r=0` is retained and has zero product for every owner.
5. At `r=8` and `r=48`, owners `0,1,2,3,4` have distance exactly `8` and
   are safe; owner `5` has distance zero and supplies coverage.
6. A deliberate `<=q` implementation must disagree with the owner-membership
   vectors at `r=8` and `r=48`, even though the cover union remains full.
7. `B_0=B_1` as residue sets, but the owner labels and the two product factors
   remain distinct.
8. The nonunit coordinate `H=M/2` and all its zero multiplication fibers are
   retained.
9. Reflection `r -> M-r` preserves every labelled bad predicate and swaps the
   lower and upper odd annuli without losing endpoints.

Also check `E=4`, where

```text
q=16,
M=112,
v=(1,1,27,29,55,56),
nu_2(product_i v_i)=3,
```

only as a boundary/mechanism control. Two finite cases are not a universal
proof.

Malformed controls must reject noninteger `E`, `E<3`, a changed modulus,
wrong tuple length, a coordinate outside the canonical range, a deleted owner
label, weak badness, a total-gcd replacement, or a grid omitting zero.

## Accepted Prompt87 and Prompt92 impact gate

Independently verify that the accepted impact matches the frozen statements;
do not spend the main research budget reproving an already accepted route
disposition.

Prompt92 defines `E_n>=3`, `q_n=2^E_n`, and `M_n=(n+1)q_n`. Instantiate the
family with `n=6` and `E=E_6`. Audit301 proves `E_6=117`; independently
replicate this from Prompt92's exact natural-number definitions of `T_6` and
`E_6`, including

```text
C_6=21,
A_6=21^30=4640650289117164100520051333566036654601,
T_6=99465241107620972662038137293510731,
2^116 <= T_6 < 2^117.
```

Prove that the family definitions then coincide exactly with
`q=q_6=2^117` and `M=M_6=7*2^117`; then identify these consequences:

- `AGGREGATE-DYADIC-C2-COVER-DIVISIBILITY` is `refuted-exact` because every
  premise holds and `q_6` does not divide the labelled product.
- Its exact contrapositive is refuted by the same tuple: product
  nondivisibility holds but no common safe fixed-grid residue exists.
- The conditional C2 chain fails exactly where it applies the aggregate
  lemma. It cannot obtain `q_n|product_i v_i` or lift that divisibility to the
  speed product.
- Prompt87's proposed aggregate-dyadic surviving edge and the corresponding
  aggregate branch of C2 are killed at their frozen quantifiers.

Audit301 already accepts these conclusions as `proved-math-qualified` and
recommends the exact Prompt87/Prompt92 aggregate targets as refuted. Preserve
that scope and label without promoting it further.

Do not claim that the tuple refutes:

- unrestricted LRC or the accepted positive-integer/real equivalence;
- the elementary definitions or inequalities for `T_n,E_n,q_n,M_n`;
- deletion-gcd lifting, counterexamplehood-to-cover, modular product
  congruence, or pivot-witness lifting when their premises hold;
- the external MSS strict-product theorem;
- Prompt87's separate distinct-prime/obstruction route; or
- any theorem whose premise requires positive injective speeds rather than an
  arbitrary labelled residue tuple.

The earlier Prompt87 family
`(1,4,5q/2-1,5q/2)` satisfied aggregate product divisibility and therefore did
not refute the aggregate edge. Keep it separate from the new six-coordinate
family. Do not retroactively change the accepted scope of its audit.

## Frozen Prompt94 comparison

Prompt94's operative target is exactly:

```text
For every n>=2 and every positive injective integer speed tuple a,
OmegaUnit(a)>0,
```

where each pivot uses its own modulus `M_j=(n+1)a_j`, its candidate row
excludes multiples of `n+1`, and its uncovered numerator must be a unit modulo
the entire pivot modulus.

For every `E>=3`, define the derived tuple

```text
a_E=(1,q,C-1,C+1,H-1,H).
```

Prove its entries are positive and strictly increasing. It is therefore in
Prompt94's exact domain with `n=6,N=7`. At the labelled pivot of speed `q`,

```text
M_q=7q=M,
nonpivot owner speeds=(1,C-1,C+1,H-1,H)=w.
```

The accepted full-grid cover by `w` restricts to Prompt94's candidate row

```text
R_q={r mod M : 7 does not divide r}.
```

Therefore this exact transfer proves

```text
Q_q(a_E)=empty,
DefUnit_q(a_E)=0.
```

It does **not** prove `OmegaUnit(a_E)=0`, because Prompt94 sums the nonnegative
deficits of all six pivot rows. Do not silently replace the all-pivot sum by
the selected `q` row.

Mandatory literal sanity check at `E=3`:

```text
a_3=(1,8,13,15,27,28),
DefUnit rows by increasing speed=(0,0,8,6,18,14),
OmegaUnit(a_3)=46.
```

Thus `a_3` confirms the `q`-row transfer while satisfying, not refuting,
Prompt94's positive-total conclusion. Reproduce every row deficit directly or
by two exact methods if computation is used.

The family refutes the stronger assertion that every pivot row has positive
unit deficit. Record that as
`EVERY-PIVOT-UNIT-DEFICIT-STRENGTHENING-REFUTED`, not as a Prompt94
counterexample.

For the exact Prompt94 route, do one of the following:

- exhibit and fully verify an `E>=3` with `OmegaUnit(a_E)=0`;
- prove an unrestricted transfer theorem closing Prompt94's full
  `OmegaUnit>0` target;
- prove an unrestricted lemma strictly narrowing its first open edge; or
- prove the exact selected-row transfer and explain why no full-sum conclusion
  follows from the available data.

A scan of finitely many `E` values cannot establish the all-E or unrestricted
alternative.

## Frozen Prompt95 comparison

Prompt95's operative target is exactly
`MAX-COVERED-PIVOT-INTERNAL-COVER-REDUNDANCY` for every positive injective
integer speed tuple. It uses pivot-dependent moduli, candidate rows excluding
multiples of `N`, the set of fully covered pivots `F(a)`, the unique
maximum-speed covered pivot `j_star`, and labelled internal complete covers.

The same injective `a_E` is in Prompt95's exact domain. The transfer above
proves that the speed-`q` pivot is covered, so

```text
q in F(a_E).
```

Also prove directly that the speed-`1` pivot is covered: its modulus is `7`,
and owner speed `H=7*2^(E-1)` has zero product on every candidate. Hence

```text
{1,q} subset F(a_E).
```

Do not infer that `q=j_star`. Compute or prove the status of every other pivot
`C-1,C+1,H-1,H`, determine the exact set `F(a_E)`, and only then apply the
unique maximum-speed definition of `j_star`. A response may use an exact
deterministic computation to establish a particular `E`, but a universal
all-E claim requires a symbolic proof.

Mandatory `E=3` sanity check:

```text
a_3=(1,8,13,15,27,28),
F(a_3)={1,8} by speed label,
j_star=8.
```

At this instance, the five-owner `q`-row cover is

```text
w=(1,13,15,27,28).
```

Only owner speed `1` lies in `F(a_3)\{8}`. Therefore this cover is **not** an
internal Prompt95 cover. Explicitly show that owner `1` alone does not cover
`R_q`; for example, candidate `r=q` is safe for owner `1`. The fixed-grid
minimal-cover/private-residue claim, even if independently proved, does not
refute Prompt95.

For the exact Prompt95 route, do one of the following:

- find an `E>=3`, prove all of `F(a_E)` and `j_star`, and exhibit an internal
  complete cover at `j_star` with no redundant owner;
- prove an unrestricted transfer theorem closing Prompt95's redundancy target;
- prove an unrestricted lemma strictly narrowing its first open edge; or
- prove the exact family separation, including every `F` and `j_star` fact on
  which it relies.

Do not substitute the known five-owner cover for the required internal cover.

## Anti-equivalence and anti-repair gates

Audit every attempted implication against this diagram:

```text
audit301 accepted Prompt92 family
        |
        | concise deliberate independent replication gate
        v
confirmed fixed-grid owner cover w at modulus 7q
        |
        +--> accepted E=E_6 refutation of Prompt87/92 aggregate bridge
        |
        +--> derived injective a_E, pivot q, nonpivot owners w
                                      |
                                      +--> Q_q empty and DefUnit_q=0
                                      |       |
                                      |       +--> no OmegaUnit conclusion
                                      |
                                      +--> q in F(a_E), 1 in F(a_E)
                                              |
                                              +--> determine every other F row
                                                   before j_star/internal cover
        |
        +--> five-owner fixed-grid minimality, if proved
                |
                +--> not Prompt95 internality without F/j_star premises
```

The following are forbidden:

1. Repairing Prompt92 by changing dimension, exponent, grid, strictness,
   deletion gcd, repetitions, product conclusion, or quantified tuple domain.
2. Adding a counterexample-origin, injectivity, height, unit, primitive, or
   selected-pivot premise to salvage aggregate divisibility.
3. Treating the family as a positive-integer LRC counterexample.
4. Inferring `OmegaUnit=0` from `DefUnit_q=0`, or Prompt95 failure from the
   five-owner cover without computing `F(a_E)` and `j_star`.
5. Calling a family-specific identity an unrestricted P94/P95 lemma.
6. Reviving the stopped Prompt93 `Psi` score or importing a common-clock
   denominator.
7. Using Prompt91 Fourier/Fejer, Prompt89 junction, Prompt90 uncrossing,
   Prompt92 repair, packing, or another open route as a hidden premise.
8. Offering a bounded scan, numerical plot, or response-authored attachment
   as the symbolic proof.

## Adversarial attack lanes

### Lane A - concise accepted-result replication

Reproduce the all-E algebra, strict table, `E_6`, and killed-arrow map from the
definitions. Name audit301 as the accepted comparison target. Stop after the
gate passes; repetition is not synthesis.

### Lane B - injective tuple and exact q-row transfer

Prove positivity and injectivity of `a_E`. Identify its pivot-q row
definitionally with the accepted fixed-grid cover by `w`, then derive
`Q_q=empty` and exactly `DefUnit_q=0`. State why neither conclusion is
`OmegaUnit=0`.

### Lane C - Prompt94 all-row adversary

Analyze the other five Prompt94 rows. Reproduce the exact `E=3` deficits and
`OmegaUnit=46`. Seek a valid exact counterexample, complete transfer, or
unrestricted narrowing lemma; otherwise prove the selected-row separation and
name the first all-pivot obstruction.

### Lane D - Prompt95 F/j_star and internality adversary

Compute or prove every pivot's membership in `F(a_E)` before naming `j_star`.
Reproduce `F(a_3)={1,8}`, `j_star=8`, and the noninternality of `w`. Seek a
valid exact counterexample, complete transfer, or unrestricted narrowing
lemma; otherwise give the exact family separation. A new broad search is out
of scope.

## Optional computation boundary

No code attachment is required. Exact computation may support the mandatory
`E=3` row tables and exploration within the single family `a_E`, but a bounded
range of exponents is not evidence for an all-E or unrestricted claim.

If code or output is attached, provide separate source, manifest, and result
files; hash every exact file with SHA-256; state language/version, deterministic
order, domain, exit status, completion marker, and first failure. Label all
such material `unverified response input` until recovery and independent
replay. Missing, stale, mismatched, partial, or chat-only artifacts are
`not-run`, never computed finite evidence.

## Exact mutually exclusive disposition semantics

Return exactly one disposition, applying this priority order so categories
cannot overlap:

1. `STOP` if deliberate replication disagrees with audit301, the `a_E`
   transfer fails, a mandatory control is inconsistent, or the turn cannot
   complete a synthesis claim beyond known replication. Report the first
   exact failed arrow. Known Prompt92 replication alone is `STOP`, not a new
   result.
2. `COUNTEREXAMPLE` if, after the gate passes, one exact `a_E` instance (or an
   explicitly justified tuple derived within this narrow family) satisfies
   every premise and refutes the exact Prompt94 or Prompt95 operative target.
   Failure of a stronger per-row or fixed-grid statement does not qualify.
3. `TRANSFER` if there is no such counterexample and a proved unrestricted
   theorem closes the full first open target of Prompt94 or Prompt95.
4. `PIVOT` if neither prior category applies and one proved unrestricted lemma
   strictly narrows a first open P94/P95 edge without adding an equally strong
   existential or family-specific premise.
5. `SEPARATION` only if none of the prior categories applies and the response
   completes the exact `a_E` q-row transfer, both mandatory `E=3` controls,
   every `F`/`j_star` fact it invokes, and a rigorous explanation of why the
   family does not refute or close Prompt94/95.

State the scope of `SEPARATION` exactly. An `E=3` proof is an `E=3`
separation only; an all-`E` separation requires symbolic control of every
relevant pivot row for every `E>=3`.

No disposition promotes evidence. Audit301 inputs remain
`proved-math-qualified`; new response reasoning remains response-authored
until independent audit.

## Required response schema

Return exactly these sections.

1. **Disposition** - `STOP`, `COUNTEREXAMPLE`, `TRANSFER`, `PIVOT`, or `SEPARATION`.
2. **Evidence boundary** - audit301 accepted scope versus quarantined material.
3. **Concise independent replication** - definitions, strict table, valuation, `E_6`, and exact route impact.
4. **Derived injective tuple** - positivity, injectivity, labels, and every pivot modulus.
5. **Exact q-row transfer** - owner correspondence, candidate restriction, cover, `Q_q`, and `DefUnit_q`.
6. **Prompt94 E=3 control** - all six deficits and `OmegaUnit=46`.
7. **Prompt94 synthesis** - exact counterexample, transfer, pivot, or selected-row separation.
8. **Prompt95 E=3 control** - all pivot cover statuses, `F={1,8}`, and `j_star=8`.
9. **Prompt95 internality audit** - owner membership in `F\{j_star}` and the failed singleton cover.
10. **Prompt95 synthesis** - exact counterexample, transfer, pivot, or family separation.
11. **Fixed-grid minimal-cover audit** - accepted cover versus unverified private-residue claim.
12. **Dependency graph** - accepted family through `a_E` and both route branches.
13. **Optional artifact ledger** - hashes/quarantine labels or `not-run`.
14. **Proof-obligation ledger** - claim, dependency, evidence class, falsifier, and owner.
15. **Final evidence boundary** - accepted input, response reasoning, finite computation, and open routes.

## One-turn budget and fail-closed rules

Use at most 15 percent for independent Prompt92 replication, 35 percent for
the injective transfer and Prompt94 all-row analysis, 35 percent for Prompt95
`F`/`j_star`/internality analysis, and the remaining 15 percent for the exact
disposition, dependency graph, and evidence ledger. A strict majority of the
turn belongs to the derived injective tuple and P94/P95 route impact.

Return `STOP` immediately on:

- any noncanonical family coordinate or deletion gcd other than one;
- any uncovered canonical residue for one `E>=3`;
- failure of the exact valuation `E-1`;
- a weak/strict swap, omitted zero, lost cyclic side, or deduplicated `1`;
- use of `E=2` as though it were inside the family;
- failure to instantiate Prompt92 at its actual `E_6`;
- confusion between the repeated Prompt92 tuple `v` and the injective tuple
  `a_E`;
- any claim `OmegaUnit=0` derived only from `DefUnit_q=0`;
- any Prompt95 `j_star` or internal-cover claim made before establishing all
  required membership facts in `F(a_E)`;
- a Prompt94 or Prompt95 conclusion without its own frozen premises;
- an invented repair of Prompt92 after its exact failure;
- any mismatch in the full recovery commit/path/hash identities;
- treatment of an attachment or bounded replay as a universal theorem; or
- any claim that unrestricted LRC is proved or refuted.

Known family replication is only the admission gate. Preserve Prompt94 and
Prompt95 as `open` unless their exact operative statements are separately
proved or refuted; a completed exact family nontransfer map is `SEPARATION`.
