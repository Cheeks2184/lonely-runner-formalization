# Renault 5.1 exact finite certificate result

Status: one predeclared bounded discovery run and one separately authorized fixed
verification completed successfully. This is exact finite evidence; the Lean
continuum theorem and six-total assembly remain unproved.

Root read the entire frozen plan, its endpoint/mask implication and all three
proposed programs before authorizing execution. Plan SHA-256:
`7f9d296d61db16467e7b7bf34e5070b7497f3b6a0be939e2e8fa273887ecf4af`.
The earlier plan's UNEXECUTED descriptions are its historical predeclaration;
this result report records the subsequent execution separately.

## Result and exact scope

The fixed 120 atoms yielded 98 distinct good-candidate masks and 41 inclusion-
minimal masks. Every one of the 12,341 unordered retained triples with repetition
has a supplied common candidate among the exact 23 alternatives of Renault5.1.
The last three alternatives retain strict safety. The proof target is arbitrary
three real phases with twenty closed dilation/shift choices or three strict
translation choices; it is not a tuple of arbitrary runner speeds.

Discovery performed 2,760 representative predicates, 918 true-bit open-interval
endpoint checks, 9,506 ordered class comparisons and 4,920 dominator comparisons.
These are the recorded actual counts; the predeclared maxima remain in the plan.
The retained triple count is choose(43,3)=12,341, including repeated indices.

The separately launched verifier independently reconstructed the cuts and masks,
checked every true open-atom endpoint certificate, the quotient map, retained
antichain, domination and ties, and all supplied triple witness bytes. It did not
search for replacement witnesses or rerun discovery. Its positive result is
recorded verbatim below.

```json
{"atoms": 120, "candidate_predicates": 2760, "classes": 98, "kernel_theorem": false, "retained": 41, "triple_witnesses": 12341, "verified": "fixed-coverage-certificate"}
```

For continuum soundness, each singleton keeps its exact strict/closed test.
For an open atom, non-strict rational endpoint bounds and positive candidate
slope imply strict safety at every interior point. The supplied dominators
reduce arbitrary atom triples to retained triples. These manuscript implications
still need Lean implementations and kernel-checked concrete certificate data.
A Python success flag, hash or generated numeral alone supplies no trusted premise.

## Execution and reproducibility

After statically extracting and checking the three source hashes exactly as in
the frozen plan, root ran these commands in the fresh extraction directory:

```bash
python3 -I run-capped.py discover ./discovery-evidence
python3 -I run-capped.py verify ./fixed-evidence ./discovery-evidence/stdout.bin
```

The second command was authorized only after the first concrete output and
receipt were read. Both actual driver and child exit codes were 0, both streams
were captured to EOF, both stderr streams were empty, and neither run recorded
a timeout or resource/capture failure. No larger retry or widened domain occurred.
The exact JSON output of discovery is preserved byte-for-byte in
`research/astra-renault-phase-five-one-certificate.json`; its SHA-256 is
`c1374409d4f6804487179fe9765695a2f0d756ccd2a79b5ee8616e919acbb7b1`.
Its size is 19,034 bytes. The verifier consumed these exact bytes as stdin.
Full receipts and the verifier's exact stdout/stderr are in
`research/astra-renault-phase-five-one-execution.json`.

Recorded child wall times: discovery 0.022576235 seconds; fixed verification 0.017076386 seconds.
These are measurements from the capped launcher, not a prediction for Lean.
Child CPU limits were (59,60) seconds, child address space 224 MiB, driver soft
address space 32 MiB, and child wall deadline 60 seconds. Address limits are
limits, not measured memory use. Complete evidence directories were 36,281 and
36,443 bytes respectively, each below 1 MiB.

## Next bounded work

Independently review the exact final certificate and endpoint transfer. Implement
generic atom/mask soundness, then the checked cut partition, endpoint inequalities,
dominator data and all supplied triple witnesses in Lean. Plan any finite kernel
verification with explicit resource bounds before executing it. The other Renault
phase lemmas 6.1 and 6.4 have different roles/candidates; this certificate cannot
be substituted for either. The five-moving 1/6 supply, remaining divisibility
branches, canonical six-total theorem, H8 and unrestricted LRC remain outstanding.
