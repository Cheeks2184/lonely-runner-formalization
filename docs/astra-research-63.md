# Endpoint graphs and reduced arcs retain the exact supply gap

Pass62 merged through PR61 at `970295eb9ebe88498b3faa9831bac775548a7b43`.
Both required CI jobs passed; root verified the exact reviewed head on fetched
main. This pass continues uniform arbitrary-N research. No new Lean theorem
or successive-runner milestone is introduced.

Finite Euler counting handles isolated closed witnesses exactly. The reviewed
boundary formula counts simultaneous contacts once and is equivalent to
successful endpoint supply. Euler counting cannot inherit the monotonicity
of ordinary integration: a proposed lower bound fails on an explicit
unbounded family with all deletion seeds available.

The static graph of strict bad-time arcs has an exact gcd/ceiling edge count.
An empty full-good set forces connectivity and an aggregate edge bound after
contracting the common-origin clique. That edge bound alone is too weak:
the translated lcm family passes it while having full witnesses and a
disconnected graph. With an even speed, graph disconnection is exactly
ordinary full-witness existence; the all-odd case has the witness 1/2.
Deletion seeds give simultaneous colour-deletion disconnections, but no
contradiction from these conditions has yet been proved.

Merging coincident centres and removing all contained bad arcs preserves
the bad union. The remaining endpoints follow centre order, so full-good
components are precisely the nonnegative closed gaps between neighboring
surviving arcs. The exact integer determinant test includes equality and
period wrap. Omitting containment pruning is unsound: a nonneighbor can
cover an apparent gap. The canonical pruning still does not make the total
arc length at most one uniformly, as the translated lcm family demonstrates.
These are complete checking reformulations and scope constraints; they do
not supply a successful pair for every tuple.

A focused primary-source refresh preserves the distinction between ordinary
and shifted LRC and corrects a count error in the old pair-sum geometry note:
the reported shifted counterexample starts with five moving speeds, not
zonotope dimension five. No new source theorem is adopted as a uniform proof.
The independent mathematical reviews, source bindings and local workflow,
diff and storage checks are retained in `research/astra-pass63-verification.json`.
No mathematical enumeration or unchanged local Lean build is required.

The active colour-critical cover investigation seeks a genuinely restrictive
consequence of all deletion seeds and actual integer periodicity. It is a
separate unfinished task and is excluded from this checkpoint. Uniform supply,
Q and the unrestricted canonical conjecture remain unresolved.
