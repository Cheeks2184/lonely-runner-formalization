# Pass 100: positive common-time transport across arithmetic blocks

PR #98 merged `52758d2e85ca73f7daa1b579dc55dc8dc69c193a`. Both hosted
checks succeeded, with 3,731 build jobs, 710 trust reports and 32 remote
file hashes plus ancestry verified. This pass records manuscript mathematics;
it adds no Lean theorem and does not resolve unrestricted LRC.

For n >= 6 positive integer moving speeds, put N = n+1 and D = 6272n^2-2.
Suppose the original labels have a partition into b nonempty blocks, each of size at
most N/4. Require every integer relation with each coefficient of magnitude
at most D to have zero weighted sum separately in every block. The positive
transport manuscript proves that the original speeds share a strict
1/N-good set of Haar measure at least 2^(-b-1).

The proof uses finite Fourier majorants and minorants of buffered good
arcs. Each block has good measure at least 1/2 by the union bound. Exact
factorization of the finite polynomial integrals then combines the blocks
at the same original time. Charging the approximation error relative to
the positive block means keeps the required degree quadratic in n; the
final positive measure bound may still be exponential in b. Separate block
witnesses and independence of unsmoothed indicators are not assumed.

There is a unique finest partition with this splitting property. Its blocks
are the components generated by bounded relations that cannot be split
into proper zero restrictions using their existing coefficients. Using all
relation supports would also connect independent blocks through sums of
disjoint relations. Consequently any tuple whose actual maximum is at
most 1/N has a finest block larger than N/4. The missing unrestricted step
is a justified use of strict failure and full count/sum minimality to
control that large block. The native-sharp progression {1,...,n} already
has one block and minimum possible sum, so those arithmetic descriptions
alone cannot provide the contradiction.

The finite-height unit obstruction closes a separate gap in Pass 99.
At n = 3000, let A_n = n sum_(j=0)^4 binom(n-1,j), D_old = 90A_n-2,
B = 60D_old+1 and V = {6B^j,10B^j,15B^j : 0 <= j < 1000}. This tuple
is positive, distinct and primitive, every deletion gcd is one, and
max(V) <= binom(n+1,2)^(n-1), the exact sharper inclusive height cutoff.
It satisfies the complete negative fifth-Fourier inequality derived from
failure, yet no relation in the D_old coefficient box has a unit entry,
even with unrestricted support. The actual time 1/4 is a strict native
witness. Thus those listed necessary conditions still cannot force the
specified bounded unit extraction; actual strict failure and full
minimality remain essential missing premises.

The positive theorem applies to this same tuple: D < D_old and every
D_old-bounded relation splits into its triples. Its finest partition is
exactly those triples, although the graph using all relation supports is
connected. This supplies an explicit application inside the sharper height
domain, without treating the example as a counterexample to LRC or as
proof of the universal finite-family supply.

The source manuscripts and their independent reviews retain the complete
hypotheses and endpoint arguments. New analytic Lean infrastructure remains
deferred until a concrete reduction for the unresolved large block is
established. Verification for this pass checks source bindings, unchanged
Lean files and pinned dependencies, formatting and workflow consistency.
No local build or dependency-cache restoration is needed.
