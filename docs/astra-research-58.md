# Remaining parity branches and canonical six-runner assembly

The two-even and three-even branches now have compiled proofs with exactly the
planned positive common-time, closed sixth-margin contracts. They permit repeated
positive natural speeds. Each arbitrary-pivot adapter covers exactly one label
divisible by three and the stated number of even labels, without a tuple gcd
hypothesis. The one-even case was merged previously.

`ThreeEvenOrbit` supplies the scalar orbit-to-divisibility helpers. The odd-zero
obstruction forces the two even phases above one sixth and the other odd phase
above one third under an all-real no-witness hypothesis. Applying it in both odd
label orders yields divisibility and size bounds; the five-dilate obstruction
then closes the three-even case. The two-even proof applies the signed 6.1 and
6.4 phase theorems at a constrained maximum, preserving actual fractional upper
endpoints and the closed zero-shift alternative.

The final dispatcher and canonical bridge are compiled and independently
accepted after full source review. They combine the primitive divisor-count cases, remove the common gcd, extend
H7 through five moving natural speeds, and use the existing bounded real bridge
for six total runners. H7 is a hypothesis index, not a seven-runner milestone.

## Verification and delivery evidence

Focused parity checks passed. The three-even odd-zero and extension receipts
retain every physical pre-check source snapshot and full raw output. The
historical Orbit receipt and the first two failed two-even checks lack some
original execution evidence; those limits are explicit and were not reconstructed.
The last failed two-even worker check and the successful Astra repair both use
an automatic physical-capture driver. Independent final source reviews and the
normal root integration/trust checks have passed: 3,728 build jobs and 700
theorem reports. All four final declarations use only `propext`,
`Classical.choice`, and `Quot.sound`. The final independent canonical review
accepted the full proof bodies and all real-speed quantifiers; root verified
its 22 source bindings and both complete retained logs.

The three-even obstruction needed two worker repairs; its witness extension
needed one worker repair and one Astra pre-check guidance round. The two-even
worker used two repairs and one Astra guidance round, then handed off the remaining
cast, fraction, and finite-set errors. One direct Astra repair added 26 and removed
15 source lines and compiled successfully. Per-task model tokens and total worker
elapsed time remain unavailable; these task counts do not measure Astra savings.

Three earlier returns were due to exhausted inherited worker contexts, including
one partial two-even draft. Fresh explicit Terra High contexts completed the
subsequent drafting/check work. Root also corrected an orchestration mistake:
messages to an already completed worker did not restart its turn; an explicit
follow-up did. These are recorded as workflow overhead, not mathematical failures.

PR56, the previous phase-certificate checkpoint, merged at
`34afe4fcf77e06efd49f659edb9db261385ad385` after both required checks passed;
remote and local main ancestry were verified. This pass preserves its receipt.
The shared cache remains needed by immediately queued uniform research; its latest report
measured about 7.9 GB, with no cleanup overlapping checks.

## Goal boundary

Canonical N6 is now established. `sixRunners` covers every injective real-speed
family on six labels and every selected runner at the closed 1/6 margin.
The theorem, full build, trust audit and independent exact source review have
passed. This is a transition milestone, not a resolution of unrestricted LRC.
Research now turns to a uniform arbitrary-N mechanism.
Successive individual runner counts are not the roadmap. The unrestricted goal
remains active and unresolved.
