# Forty-ninth Astra checkpoint: affine pair bounds

Nine proved declarations and a ten-field proposition connect the anchored
pair bound to the safe folded phases needed by the two-divisible-three
argument. A separately reviewed manuscript isolates the final implication
from a supplied factor-six divisibility cover to a positive witness.

## Exact phases and transported bounds

The sixth-affine fractional-part identity allows arbitrary real time and
signed integer coefficients. Its only residue premise is the exact integer
decomposition of the natural speed. Nondivisibility by three gives the four
centered residues modulo six, with the quotient adjusted for residues -1
and -2. Integer dilation of fractional parts is a specialization.

A supplied bound on the anchor's five-sixths fiber transfers to all three
odd-sixth phases. One third shift moves the anchor to five sixths while
preserving both measured norms. The bound may be zero; neither existence
of a maximizing seed nor positivity of the time is asserted by this lemma.

## Ten safe folded phases

The scalar intervals from the preceding checkpoint imply ten named closed
sixth-margin bounds. Two further strict inequalities show that triple and
quintuple dilation improve a positive norm below one sixth, and doubling
followed by a half-turn makes any strictly bad phase strictly safe.

At an anchored time with a positive smaller pair norm below one sixth, the
supplied fiber bound applies at the triple and quintuple times. Strict
improvement of the smaller norm forces the two corresponding larger-norm
constraints. The scalar band theorem then supplies all ten safe bands.
The proof preserves actual phase orientations through the folding identity.
Its supplied bound, ordering and positive-small-norm premises remain explicit.

The generic finite-set pigeonhole theorem selects two distinct labels sharing
one of two covering predicates whenever the set has at least three members.
The predicates may overlap, and repeated speed values remain distinct labels.

## Verification and remaining assembly

The full source build passed 3,695 jobs. The trust audit accepted 623 dependency
reports and eight axiom-free declarations. Independent source reviews checked
all nine exact contracts, the proposition's ten-input constructor and every
field projection. Root read both complete reports and replayed their exact
bounded import checks; all guards and allowed-axiom checks passed. Complete
logs and failed drafts are preserved in the verification JSON.

Elaboration corrections included the transport product shape and the implicit
finite-set argument of the filter partition theorem. The final pair-bound
assembly compiled unchanged on its first check. No mathematical statement
was weakened to resolve a compiler error. New modules compile without warnings;
the full build still replays existing dependency warnings.

The conditional cover manuscript and its independent review are included
with a passing root replay of ten source bindings and three exact proposed
contracts. It proves the terminal ratio and two-seed argument assuming the
factor-six cover. This manuscript review is separate from a kernel proof of
that implication, whose source implementation belongs to the next checkpoint.

The boundary-minimum-zero argument must still establish the cover. The full
two-divisible branch, an unrestricted five-moving sixth-margin theorem, a new
canonical runner count, and unrestricted LRC remain unresolved here. Active
and queued source verification continue to need the shared pinned cache.

The preceding checkpoint merged through PR47 after both required checks passed,
at main commit `f7c77c8d2506eebf386aac2151d97bfa05913828`. Root verified
reviewed-head ancestry and synchronized local and remote main before this pass.
