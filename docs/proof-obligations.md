# Proof-obligation ledger

Statuses: `proved-lean`, `proved-math`, `literature`, `open`, `rejected`, and
`blocked-external`.

| ID | Obligation | Status | Evidence / next action |
|---|---|---|---|
| STMT-01 | Define circle distance and the closed lonely boundary. | proved-lean | `UnitCircle`, `circleDistance`, and `LonelyAt` use mathlib's quotient metric and `≤` against the distance. |
| STMT-01A | Complete the reusable circle-distance range, symmetry, and periodicity library. | open | `circleNorm_add_int` and the one-Lipschitz estimate now support insertion; explicit range and general symmetry/translation lemmas remain useful for later analytic work. |
| STMT-02 | Encode the canonical `forall runner, exists time` statement for injective real speeds. | proved-lean | `LonelyRunner.Conjecture`; unrestricted real time, total-runner denominator `N`, and closed boundary. The nonnegative-time and relative-velocity equivalences also compile. |
| STMT-03 | Prove equivalence with a distinct nonzero stationary tuple. | proved-lean | `conjecture_iff_distinctStationaryConjecture`; `Fin.lastCases` appends the stationary runner and `Fin.succAbove` enumerates exactly its complement while preserving the denominator. |
| STMT-04 | Extend the stationary tuple statement to repeated nonzero entries without hiding lower-dimensional induction. | proved-lean | `distinctStationaryConjecture_iff_stationaryConjecture` and the composed `conjecture_iff_stationaryConjecture`; the finite range is explicitly enumerated, its cardinal `m` satisfies `1 ≤ m ≤ n`, and the stronger `1/(m+1)` bound is weakened to `1/(n+1)`. |
| RED-01 | Reduce arbitrary real speeds to rational/integer speeds with exact closed boundary. | proved-lean | `conjecture_iff_positiveIntegerConjecture` in `BHKRealReduction.lean`. The proof formalizes BHK's rational relation-space collision, strict magnitude-cardinality drop, generalized Kronecker closure transfer through an open strict-good cube, the rational-collinear branch, sign normalization, denominator clearing, and repeated-tuple reduction. It is an equivalence theorem, not a proof of the integer conjecture. |
| RED-01A | Prove exact common-denominator clearing and repeated-tuple reduction between positive rational and natural formulations. | proved-lean | `rationalCommonDenominator`, `clearRationalSpeed`, its cast/positivity/injectivity lemmas, `distinctPositiveRationalConjecture_iff_positiveRationalConjecture`, and both composed integer equivalences in `LonelyRunner/RationalReduction.lean`. The proof rescales time by the positive common denominator, enumerates the finite range of repeated tuples, and weakens the stronger lower-dimensional bound while preserving every quantifier and closed threshold. |
| RED-01B | Prove the BHK-sufficient reverse multidimensional Kronecker inclusion. | proved-lean | `exists_mFourier_separating` and `orbitHom_mem_closure_range_of_relations` in `LonelyRunner/KroneckerSeparation.lean`. Supporting modules construct the continuous orbit subgroup, prove the exact character/relation iff, normalize subgroup Haar measure, establish character orthogonality, and separate a closed subgroup from a translated coset. Two independent adversarial audits found no gap; axiom probes report only `propext`, `Classical.choice`, and `Quot.sound`. |
| RED-01C | Construct BHK rational coordinate rows and the adjacent-ratio collision. | proved-lean | `RationalRelationSpace.lean` reconstructs velocities from rational coordinate rows; `PositiveRationalRelation.lean` obtains a positive compatible rational combination by finite-product density; `BHKNongenericRow.lean` detects irrational ratios; and the `BHKAdjacent*`, `BHKBridgeAssembly`, and `BHKPositiveCollision` modules produce a nowhere-zero compatible tuple with strictly fewer distinct magnitudes. |
| RED-01D | Transfer the lower-dimensional rational collision witness back to the original real orbit without losing the closed endpoint. | proved-lean | `exists_stationaryWitness_of_rational_collision` uses the strict margin `1/(m+1) < 1/m ≤ 1/(d+1)`, an open strict-good torus set, and `orbitHom_mem_closure_range_of_relations`. `BHKRealReduction.lean` composes both irrational and rational-collinear branches and removes signs. |
| RED-02 | Remove signs and common integer factors. | proved-lean | `circleNorm_mul_abs_right`, `exists_stationaryWitness_abs_iff`, `exists_stationaryWitness_scale_iff`, and `exists_stationaryWitness_natScale_iff` in `LonelyRunner/Normalization.lean`; these are fixed-instance normalizations, not the real-to-integer reduction. |
| BASE-01 | Prove the one-moving-runner case constructively. | proved-lean | `LonelyRunner.oneMovingRunner`, with witness `t = 1/(2v)` and the exact half-period norm theorem. |
| BASE-02 | Prove the two-moving-runner case without importing the conjecture itself. | proved-lean | `twoMovingSpeeds_of_abs_le`, `twoMovingSpeeds`, and `twoMovingRunners`: ratio at least `2` uses fast insertion from `oneMovingRunner`; ratio in `[1,2]` uses `minimumScaleResidueBands`. This is the full three-total-runner case. |
| PART-01 | Prove the quantitative fast-runner insertion theorem mathematically. | proved-math | Exact proof and endpoint audit in `docs/fast-runner-insertion.md`. |
| PART-02 | Formalize the fast-runner insertion theorem in Lean. | proved-lean | `abs_circleNorm_sub_circleNorm_le`, `exists_small_shift_circleNorm_ge`, and `fastRunnerInsertion` in `LonelyRunner/FastRunnerInsertion.lean`. |
| PART-03 | Prove the minimum-scale residue-band structured class. | proved-lean | `circleNorm_ge_of_int_band`, `circleNorm_ge_of_abs_int_band`, and `minimumScaleResidueBands` in `LonelyRunner/StructuredClasses.lean`; the explicit witness and both closed endpoints compile. |
| PART-04 | Prove the two irrational rank-one-block structured class. | proved-math | Open good arcs plus an explicit reduction to irrational-rotation density in `docs/structured-classes.md`; the standard density dependency is cited to Weyl (1916), and Lean formalization remains. |
| PART-05 | Prove soundness of finite bad-set union and ordered selected-parent overlap certificates. | proved-lean | The five finite-set theorems in `LonelyRunner/ModularCertificates.lean`, culminating in `exists_mem_avoiding_ordered_of_sum_lt_card`, prove that either strict certificate leaves an uncovered candidate. |
| PART-06 | Formalize modular pivot candidates, bad sets, candidate cardinality, and the closed-boundary witness bridge. | proved-lean | `cyclicResidueDistance`, `pivotCandidates`, `pivotBadResidues`, `card_pivotCandidates`, `circleNorm_nat_div_ge`, and `pivotResidueWitness` in `LonelyRunner/PivotResidues.lean`. |
| PART-07 | Formalize the exact modular pivot bad-set gcd/ceiling count. | proved-lean | `card_pivotBadResidues_exact` in `LonelyRunner/PivotCounts.lean`; its supporting theorems prove the strict cyclic-ball count, multiplication-period image, uniform gcd fibers, and the exact subtraction for residues divisible by `N`. |
| PART-08 | Prove that pivot grids are complete for any fixed positive-integer instance having a witness. | proved-lean | `exists_boundary_witness`, `exists_safe_pivot_residue`, and `exists_witness_iff_exists_pivot_certificate` in `LonelyRunner/PivotBoundary.lean` use a real interval, finite minimum, IVT, both signed boundary branches, and Euclidean residue normalization. The hypotheses explicitly include a finite anchor, positive speeds, and `2 <= N`. |
| PART-11 | Identify the positive-integer conjecture exactly with uniform finite pivot-certificate existence. | proved-lean | `circleNorm_nat_div_eq` and `not_mem_pivotBadResidues_iff_circleNorm_ge` give the exact strict-boundary interface; `positiveIntegerConjecture_iff_pivotCertificateConjecture` preserves `1 <= n`, injectivity, positivity, and `N=n+1`. This is not a real-to-integer reduction and does not prove uniform certificate existence. |
| PART-12 | Derive fixed-pivot Fourier coefficients for the candidate filter and each arithmetic bad set. | proved-math | `docs/fourier-pivot-badsets.md` gives the normalized subgroup transform, strict Dirichlet kernel, gcd-supported multiplication pullback, and candidate-filter convolution. An independent endpoint/normalization audit and exhaustive finite numerical cross-check found no mismatch. The absolute main-term criterion is now explicitly refuted even for a certified three-speed pivot; the formulas are not yet in Lean. |
| PART-09 | Prove the pair-sum critical spectrum for a fixed positive-integer tuple. | proved-math | Opposite active triangular-wave slopes at a maximizer give denominator `a_p+a_q`; the `M=1/2` cusp is separate. The invariant bound is `max_{p<q}(a_p+a_q)`; `2a_n-1` requires sorted distinct speeds. See `docs/round6-strategies.md`. |
| PART-10 | Prove codimension-one divisor insertion conditional on lower-dimensional LRC. | proved-lean | `codimensionOneDivisorInsertion` in `LonelyRunner/DivisorInsertion.lean`; translated-grid and coprime-step lemmas construct the shift, and the lower-dimensional witness, factorization `D=g*m`, coprimality, and closed inequality remain explicit premises. The deletion-gcd-one corollary is restricted to minimal dimension or an established previous dimension. |
| PART-13 | Combine fast, divisor, band, and pivot branches into an exact integer induction interface. | proved-lean | `exists_appended_witness_of_integerInsertionCover` proves the fixed-step synthesis. `pivotInsertionCase_iff_not_allPivotCandidatesCovered` identifies exact pivot noncoverage, while `positiveIntegerConjecture_iff_divisorOrUncoveredPivotPrinciple` proves that the sharp divisor-or-uncovered-pivot dichotomy is equivalent to the remaining positive-integer conjecture. This is a reduction theorem, not a proof of the dichotomy. See `docs/integer-induction-cover.md`. |
| PART-14 | Formalize overlap credit without double counting. | proved-lean | `capacitatedHall_iff_existsInjective` in `HallCredits.lean` proves the exact finite capacitated Hall criterion using cloned quota units. The full-cut and ordered-union lemmas convert feasible quotas into sound overlap credit. Exact pivot bad sets show that checking only cuts through sizes 2, 3, or 4 is insufficient; the full subset family remains necessary. |
| PART-15 | Encode finite pair-sum witnesses and connect them exactly to circle distance. | proved-lean | `PairSumCertificate`, `pairSumCertificate_iff_pair_time`, and `positiveIntegerPairSumCertificateConjecture_imp_positiveIntegerConjecture` in `PairSumGeometry.lean`. Universal unrestricted pair-sum existence remains open; four restricted pair-selection rules have exact small counterexamples. |
| PART-16 | Formalize fiberwise lower bounds for ordered bad-set overlap. | proved-lean | `fiberCredit_le_card_inter_biUnion` and `exists_mem_avoiding_of_balanced_fiberCredit` in `FiberCredits.lean` prove the generic disjoint-fiber maximum-parent bound and balanced ordered-union certificate. This validates the certificate, not its uniform arithmetic premise. |
| PART-17 | Formalize a two-level anchored subfiber strengthening. | proved-lean | `fiberCredit_le_twoLevelFiberCredit_le_card_inter_biUnion` proves that anchor coverage plus the best non-anchor contribution on each residual subfiber dominates one-level credit and remains bounded by true overlap. `exists_mem_avoiding_of_twoLevelFiberCredit_sum_lt_card_of_parent_subset` converts selected earlier-parent credits into an ordered-union avoidance certificate, explicitly supporting fixed front-loaded anchors while ignoring nonanchor predecessors. The saturated modular simplification is also formalized. |
| PART-18 | Derive the exact ranked-fiber random-order identity. | proved-math | `docs/ranked-fiber-averaging.md` proves the finite averaging identity and `tests/test_fiber_average.py` checks it by literal enumeration of every order on a nontrivial modular instance. Lean formalization is in progress; no arithmetic strictness is inferred from the generic identity. |
| PART-19 | Formalize the tie-safe ranked tail calculation. | proved-lean | `average_rankedTailCredit` proves the exact per-fiber tail average over insertion codes, and `exists_code_cost_lt_of_rankedTailAverage` extracts a strict deterministic code from a strict rational average. Decoding codes to a shared permutation across all children/fibers remains part of PART-18. The false `RF-UNIF` premise is not assumed. |
| PART-20 | Formalize the exact two-equation simultaneous-congruence count. | proved-lean | `simultaneousCongruenceCompatible_iff_nonempty` proves that `b*r=x` and `c*r=y` modulo positive `M` are solvable exactly when `g=gcd(M,b,c)` divides both targets and `c*x=b*y` modulo `M*g`; `card_simultaneousCongruenceResidues` gives cardinality `g` in the compatible branch and zero otherwise. The stronger cross modulus is essential. |
| PART-21 | Derive and derandomize biased exponential-clock ordering. | proved-math | `docs/adaptive-order-heuristics.md` proves on paper that rates `lambda_i` give tail probability `Lambda/(lambda_i+Lambda)` and that memorylessness makes the prefix potential a rate-weighted average of next-step potentials. `search_adaptive_orders.py` checks that recurrence exactly at every step and produces a deterministic order. The algebraic extraction is PART-22; the exponential/potential identification remains outside Lean. The GCD-rate specialization's uniform premise is separately rejected in UNIF-09. |
| PART-22 | Formalize finite weighted conditional-expectation extraction. | proved-lean | `exists_nextPotential_le_of_eq_weightedAverage` extracts a nonincreasing next branch from an exact positive rational weighted average. `exists_removalOrder_terminalPotential_le` iterates this to a duplicate-free complete removal order. The theorem assumes, rather than proves, the GCD-clock potential identity and makes no arithmetic uniformity claim. |
| PART-23 | Specialize strict pivot bad sets and pair overlaps to target fibers. | proved-lean | `biUnion_pivotTargetFiber_eq_pivotBadResidues` gives the exact disjoint strict-ball decomposition. `divisibleSimultaneousCongruenceNatResidues_eq_image` proves the `q -> N*q` bijection for raw solutions divisible by `N`; `candidateSimultaneousCongruenceCount_exact` subtracts the corresponding smaller-modulus compatibility/gcd count; and `card_pivotTargetFiber_inter_pivotBadResidues_exact` sums these closed counts over the strict target ball. No ordering premise is assumed. |
| PART-24 | Isolate and test the one-prefix conditional-clock rescue criterion. | proved-math | `gcd_clock_first_step_values` reports every exact potential `V_i` after forcing one first child and verifies that their rate-weighted average is the initial potential. At pivot `8` of the UNIF-09 tuple, forcing speed `48` gives `V_48=120979207/1687191<72`; PART-22 then supplies the generic remaining-order extraction once the concrete clock interface is formalized. This rescues that instance, not the uniform conjecture. |
| PART-25 | Formalize generic two-sided relocation descent and the exact block identity. | proved-lean | `exists_cost_lt_of_decreasing_move` proves the finite descent implication for an explicit move relation and natural cost. `prefixDependentCost_relocate` proves a subtraction-free exact identity for moving one entry across a contiguous block; suffix terms cancel because their prefix sets agree. The theorem does not assume or prove the arithmetic `RELOC-UNIF` premise. |
| PART-26 | Formalize exact raw three-equation simultaneous-congruence counts. | proved-lean | `tripleCongruenceCompatible_iff_nonempty` uses `g=gcd(M,a,b,c)`, divisibility of all targets by `g`, and all three crosses modulo `M*g`; `card_tripleCongruenceResidues` gives exactly `g` compatible residues and zero otherwise. Sufficiency is an explicit four-term Bézout construction. Zero coefficients and homogeneous targets are covered; the pivot specialization is PART-27. |
| PART-27 | Specialize triple counts to pivot candidates and two-level cells. | proved-lean | `divisibleTripleCongruenceNatResidues_eq_image` proves the explicit `q -> N*q` bijection, `candidateTripleCongruenceCount_exact` subtracts the conditional smaller-modulus compatibility/gcd count, and `pivotTripleTargetFiber_eq_pair_inter_target` identifies the triple cell as a pair-target fiber intersected with a third target. Positivity and canonical-target hypotheses are explicit; no anchor-uniformity premise is assumed. |
| PART-28 | Derive and audit the anchor-star double-average implication. | proved-math | Exact identities express the two-anchor gain through pair/triple intersections and average the nonnegative third-anchor gain. If `Xi_j(h)>S_j-n*A`, successive finite averages select a strict three-anchor certificate. `audit_anchor_star.py` reproduces all reported rational values and verifies the zero-target divisor and prime-`N` cross-pivot formulas. The arithmetic uniform premise is UNIF-14. |
| PART-29 | Formalize the generic anchor-star strict-average selection step. | proved-lean | `exists_secondaryAnchors_of_anchorStar_gt` extracts an ordered distinct pair from the strict rational average. `exists_threeDistinctAnchors_of_anchorStar_gt_of_card` exposes an eligible fixed anchor, an exact cardinality-3 interface, and membership/pairwise distinctness of all selected anchors. The concrete modular average identity and uniform strictness are not assumed. |
| PART-30 | Prove and independently audit the nonzero-target Gamma dispersion bound. | proved-math | `Gamma_j(h) >= Delta_j(h)` follows from exact two-anchor credit, prefix/tail cancellation, cellwise positive-part gains, and the ordered-pair identity giving one absolute difference per unordered pair. Every cell count is an exact strict-ball sum of `candidateTripleCongruenceCount`. Separate implementations checked 65,536 arbitrary finite-set systems, 20,000 refined partitions, and 2,520 direct modular pivot-anchor cases. Concrete Lean formalization remains open; the uniform arithmetic premise is UNIF-15. |
| PART-31 | Formalize the reusable cellwise dispersion algebra. | proved-lean | `subfiber_pair_anchor_credit_eq_card_inter_union` proves exact one-cell two-parent coverage. `sum_add_directionalGain_eq_sum_max`, `directionalGains_add_reverse_eq_natPairwiseDispersion`, `cost_gain_of_additiveCancellation`, and the ordered-pair sum/average monotonicity lemmas formalize the safe natural-number algebra. The concrete global cost cancellation and fiber/cell reindexing needed to conclude the modular `Gamma >= Delta` statement remain open. |
| PART-32 | Derive the exact pair-multiplicity identity and rigorous profile moment bounds. | proved-lean | `pairMultiplicityContribution_num` and its finite-sum form kernel-check the denominator-free `PAIR-MULT` polynomial identity. `residueEnergy_sum_mod_le` formalizes modular energy balancing. For a cell profile, two further audited quadratic-energy bounds are proved mathematically. Their combined profile-free closure is nevertheless rejected by UNIF-16. |
| PART-33 | Prove residual-variation normalization invariances and audit deletion descent. | proved-math | Every row is homogeneous under common positive scaling, so a failure can be normalized to a primitive tuple. For one fixed pivot, independent signed residue replacement of nonpivots modulo the pivot modulus preserves the row. This is not a global descent: distinct representatives may collide and other pivot moduli change. Fixed-row deletion monotonicity is explicitly rejected by the RF row changing from `42/5` after deletion to `-41/7` before deletion. |
| PART-34 | Prove the range-sum profile majorization. | proved-math | For a profile, one minimum/maximum pair contributes `(r-1)R`; balancing the remaining fixed-sum integer entries adds `eta*(r-2-eta)`. Thus the full pairwise `L1` dispersion dominates the explicit `w(c)`. `rangeProfileDispersion_eq` and `rangeSumProfileLowerBound_of_cutDecomposition` kernel-check the endpoint accounting and conditional cut-energy step; existence and exact energy of the standard layer-cake cuts remain to formalize. Equality holds automatically through profile length 3. The uniform premise is rejected by UNIF-17. |
| PART-35 | Independently audit the all-pivot averaging counterexamples. | proved-math | Optimized masks, a slow literal residue/list oracle, and a separate clean-room implementation agree on all 72 pivot-anchor rows of the boundary examples `(1,14,27,40,53,66,79,92,105)` and `(1,4,5,7,8,9,10,11,17)`. They also reproduce the strict counterexample `(8,15,35,40,48,56,68,75,78)`. Strict endpoints are excluded, candidate counts are exactly `9*a_j`, and exact three-anchor/additive witnesses show that the failures do not propagate to the surviving schemes or LRC. |
| PART-36 | Prove and audit the fixed-row uncovered/debt/loss identity. | proved-lean | For a fixed pivot and first anchor, the exact range-sum margin is `Unc-(Debt+Loss)/((m-1)(m-2))`. `fixedRow_pairDeficit_add_binary_eq_neg_debt`, its finite-sum form, the cell-loss lemmas, and `fixedRow_debt_loss_decomposition` kernel-check the denominator-free algebra with explicit pair/reindexing hypotheses. Direct audit of all 72 strict-counterexample rows and 20,181 retained cells agrees. Concrete modular `hpair`/`hreindex` and the normalized uncovered/debt splitting remain to instantiate in Lean. |
| PART-37 | Prove bounded-anchor cost monotonicity under anchor extension. | proved-lean | `anchoredSubfiberCredit_mono_parents` and `twoLevelFiberCredit_mono_parents` show that available two-level credit grows with the parent set. `boundedAnchorCost_insert_le` proves exact prefix-union/tail-cost bookkeeping under explicit remaining-child monotonicity and sound-overlap hypotheses. No cardinality or nonemptiness restriction is needed abstractly. Concrete modular identification with the computed `L2` still requires target-fiber saturation/decomposition. |
| PART-38 | Prove and audit the acyclic fiber-parent selector characterization and modular witness bridge. | proved-lean | Abstractly, one token per child target chooses at most one weighted parent; zero-weight empty fibers are harmless. `AcyclicFiberSelector.lean` proves both optimization directions. `ModularAcyclicSelector.lean` instantiates the exact candidate-filtered tokens and weights, enumerates predecessor prefixes, reindexes sequence credits, and proves the natural-subtraction identity. `exists_pivotCandidate_avoiding_of_modularOrderFiberCredit` derives an uncovered candidate and `exists_real_witness_of_modularOrderFiberCredit` transports it to a real lonely time under explicit hypotheses. The deterministic implication remains sound; its proposed uniform sufficiently credited-order premise is rejected by UNIF-06. |
| PART-39 | Derive and independently audit the top-parent feedback-cycle bound. | proved-math | Restrict every positive token to its globally best-parent set. If `tau_top` is the minimum backward/feedback weight over top-parent choices and orders, then optimized cost is at most `S-F_top+tau_top`. The corrected subset recurrence includes a maximum over the last child. Independent exact implementations reproduce the stress rows and the all-pivot failure in UNIF-18. This bound remains sound although its uniform premise is false. |
| PART-40 | Quantify lower-ranked rescue and prefix-conditioned top loss. | proved-math | For every order, exact accounting gives `D(o)=S-F_top+L_top(o)-R(o)`, where `R` is the second-best rescue on tokens without an earlier top parent. Fixed-pivot rows separate top-only from unrestricted optimization. The exact prefix bound `B(P)=H(P)+sum W_e/(|T_e|+1)` over owners outside `P` with no top parent in `P` interpolates from tie-aware random order to the full top DP. Prefixes of size at most four certify the eight earlier stresses, but UNIF-18 shows that the full endpoint is not uniform. See `docs/top-parent-rescue-audit.md`. |
| PART-41 | Formalize unrestricted threshold layers, peeling, and critical soft cores. | proved-lean | `SoftFeedbackLayers.lean` uses unit levels `q<W_e`, so ties, zero weights, empty eligible sets, and owner exclusion are explicit. `orderedTokenCredit_eq_card_metOrderUnitLayers`, `tokenPotential_eq_orderedTokenCredit_add_card_trapped`, and `orderPotential_eq_orderCredit_add_orderSoftLoss` prove the exact potential/credit/loss identities. `exists_peelingList_of_localBudget` and `exists_criticalCore_of_budget_lt_every_peeling`, with their soft specializations, formalize budgeted peeling and its critical-core contrapositive. Axiom probes report only `propext`, `Classical.choice`, and `Quot.sound`. Connecting a complete peeling list to an induced `VertexOrder` and modular strict threshold remains to formalize. |
| PART-42 | Derive and audit SCC-conditioned and chain-aware soft bounds. | proved-math | A source SCC of a critical core admits one top parent per trapped token with capacity equal to its external deficit, giving the token-coupled weighted cycle obstruction. Prefix SCC condensation yields `B_core`; on `E` pivot `28`, prefix `(35,40,88)` gives exact loss `38<40`. The `k`-step nested-chain bound `B_k` satisfies `B_k<=B_1`; independent exhaustive subset verifiers reproduce all stress rows and the exact counterexample in UNIF-19. The bound remains sound, but its cross-pivot uniform premise is false. |
| PART-43 | Formalize generic repeated-block peeling and strict budget propagation. | proved-lean | `BlockPeeling.lean` proves `exists_blockPeelingList_of_localBudget` and its strict counterpart using valid prefixes, exact remaining sets, and final block length `min blockSize Q.card`. `blockPeelingBudget_oneStepBlockBudget` proves that regrouping descending one-step bounds changes no budget, and local vertex bounds recover ordinary peeling through blocks. The strict theorem explicitly requires a nonempty vertex type; axiom probes report only `propext`, `Classical.choice`, and `Quot.sound`. No modular `CHAIN3-UNIF` premise is assumed. |
| PART-44 | Derive and formalize the exact fixed-pivot Bellman potential dual. | proved-lean | `BellmanPeeling.lean` proves feasible-potential weak duality for arbitrary finite remaining sets, defines the exact recursive `optimalPeelingPotential`, proves its Bellman recurrence, feasibility, attainment by a complete order, minimum-order characterization, and greatest-feasible-potential theorem. Axiom probes report only `propext`, `Classical.choice`, and `Quot.sound`. The equivalent unit-flow formulation is proved mathematically by path decomposition. Potentials remain pivot-specific; on `F`, the unweighted full-margin sum `-75` and natural common-LCM density sum `-279450` reject those linear uniform closures. |
| PART-45 | Derive, formalize, and audit capacity-respecting whole-overlap packing beyond additive orders. | proved-lean | `OverlapCapacity.lean` proves both incidence Fubini identities and the subtraction-free theorem `|union B_i| + sum_e|Q_e| <= sum_i|B_i|` from point-use `<=mu-1`, plus the natural-subtraction corollary. Abstract injective-owner and ordered-parent lemmas recover the capacity premise for acyclic order selections. Axiom probes report only standard foundations. Cyclic packings may be stronger: an independent literal audit at `F` pivot `8` verifies 24 unique-token blocks with credit `34>32`, giving bound `70<72`; the literal union is `68`. Uniform modular existence remains open. |
| PART-46 | Characterize exact whole-block packing and its fractional obstruction. | proved-math | Binary variables choose at most one parent per token and obey one capacity constraint per residue, giving a multiple-choice hypergraph b-matching. Its LP dual prices tokens and residue capacities, but the relaxation is not integral in general: blocks `{a,b}`, `{b,c}`, `{c,a}` with unit capacities have integer credit `2` and fractional credit `3`. Ordinary max flow is exact only for singleton blocks; trimming nonsingleton blocks is invalid. Pairwise feasibility and aggregate neighborhood-capacity inequalities also have explicit abstract counterexamples. Any uniform theorem must exploit modular coset structure. |
| GEN-01 | Establish the elementary universal `1/(2n)` lower bound. | open | Formalize Haar/Lebesgue measure or a finite combinatorial equivalent. |
| FIN-01 | State and justify the published finite-checking theorem. | literature | Malikiosis--Santos--Schymura Theorem A; not yet formalized. |
| COMP-01 | Build a reproducible exact-arithmetic finite-instance checker. | proved-math | `scripts/check_integer_tuple.py` and `scripts/check_bad_sets.py`; 9 core checker tests pass, all 162 distinct tuples of 1--4 speeds from 1--8 meet the target, and the `(1,2,3,4)` bad-set inclusion--exclusion audit is exact. The checkers are exact Python, not yet kernel-verified. |
| COMP-02 | Prove the finite-extrema certificate theorem and verifier in Lean. | open | The exact Python checker enumerates common-cell endpoints and pairwise affine intersections; formal trust transfer remains. |
| COMP-03 | Reproduce the bounded residual pivot-certificate audit. | proved-math | `scripts/search_residual.py` exactly checks every primitive increasing tuple with 3--6 moving speeds and maximum speed 30; the complete regression suite now has 61 tests. The bounded-parent dynamic program is checked against literal permutation search, and the pair-sum spectrum against the exact optimizer on 784 repeated/distinct tuples. The two-parent condition covers all 305,677 residual tuples; this has no unbounded-speed consequence and is not a kernel-checked enumeration. |
| COMP-04 | Stress-test bounded-parent overlap and divisor insertion beyond six speeds. | proved-math | Exact search covers all 28,259 seven-speed residual tuples through speed 20 with two parents. At eight speeds through 15, two parents cover 1,714/1,716 and the two failures pass three-parent certificates. The divisor arithmetic condition covers 96/2,240 and 20,878/239,928 six-speed residual tuples at cutoffs 15 and 30, 925/28,259 seven-speed residual tuples through 20, and 0/1,716 eight-speed residual tuples through 15. Reproduction and counterexamples are in `docs/round6-strategies.md`. |
| COMP-05 | Audit the exact combined integer induction cover on bounded primitive boxes. | proved-math | `scripts/search_induction_cover.py` checks the three structural branches, deletion gcds, exact pivot unions, and optional bounded-parent certificates. Through eight moving speeds and maximum speed 15, all 7,211 structural residuals have every deletion gcd one and an exact pivot certificate; none has all pivot grids covered. `(1,3,4)` is the first structural-cover counterexample. Finite noncoverage is evidence only, because the all-pivot-cover implication is never tested nonvacuously. |
| COMP-06 | Test Hall-cut truncations and pair-sum selector restrictions exactly. | proved-math | `search_hall_credits.py` finds genuine cyclic bad-set examples where all cuts through sizes 2, 3, or 4 pass but the next full cut fails. `search_pair_sum_geometry.py` checks 16,368 tuples through seven speeds from `1..15`: unrestricted pairs always witness in this box, while adjacent, minimum, maximum, and extreme-only selector rules have explicit counterexamples. |
| COMP-07 | Search independently for unrestricted additive all-pivot failures. | proved-math | `scripts/search_opt_add_failures.js` literally reconstructs strict candidates/fibers and runs the unrestricted subset DP. Its complete primitive ten-speed box through maximum `22` checks all `646,646` increasing tuples, excludes exactly eleven all-even tuples, finds no all-pivot failure, and has minimum best margin `+4` on fourteen tuples. The independent output matches `certificates/opt_add_n10_max22_expected.txt` exactly. Separate targeted exact searches cover 62,693 additional evaluations across 9--11 speeds. These are bounded results, not a uniform theorem. |
| COMP-08 | Exhaustively search finite mutation domains around the CHAIN3 counterexample. | proved-math | `search_opt_add_near_chain3.js` checks 2,619 single replacements through speed 300, 3,428 valid radius-5 two-coordinate perturbations, 5,376 radius-2 three-coordinate perturbations, 291 one-speed extensions, and all 9 deletions. No all-pivot OPT-ADD failure occurs; the smallest best-pivot surplus is `+16`. Counts are per exactly defined domain and may overlap across domains. The scanner preserves the literal strict endpoint and candidate conventions; this is finite evidence only. See `docs/opt-add-near-chain3-search.md`. |
| COMP-09 | Independently audit the exact all-pivot OPT-ADD counterexample. | proved-math | For `G=(15,21,40,48,56,105,126,280,1200)`, a clean-room literal verifier reconstructs every strict candidate, token fiber, weight, and all nine `(S,F*,soft optimum,Dopt,9A)` rows. It compares a subset DP with all `9*8!=362,880` pivot-order pairs. Every row is nonstrict, with equality only at pivots `15` and `40`. Gcd, distinctness, endpoint conventions, candidate exclusion, and the direct `r=39`, `t=13/160` witness are separately checked. See `docs/opt-add-counterexample-audit.md`. |
| COMP-10 | Audit exact and certified-interval whole-block packing on the main stress set. | proved-math | `audit_whole_block_packing.py` reconstructs literal blocks/capacities, replays every integral witness, splits conflict components, and uses safe nonbinding elimination, combinatorial bounds, a grid LP-dual bound, and memoization. Exact `(need,opt,debt)` rows include `F` pivot `8` = `(32,34,36)` and `G` pivots `15,21,40,48,126`. A fixed pivot-`56` witness gives `400>392`; pivots `105` and `1200` also succeed. Hard unfinished rows are reported only as certified intervals. All eleven fixed tuples (`F`, `G`, and nine prior stresses) have a strict packing pivot; no unbounded consequence follows. See `docs/whole-block-packing-audit.md`. |
| UNIF-01 | Prove that two fixed adjacent anchors uniformly dominate all remaining modular bad sets. | rejected | Exact counterexample `(1,2,3,4,5,7)`: the proposed strict bound fails at all six pivots even though exact pivot bad unions leave witnesses; see `docs/failed-approaches.md`. |
| UNIF-02 | Convert a bounded-indegree sum of pairwise overlaps directly into ordered union credit. | rejected | The minimal abstract counterexample `R={x}`, `B1=B2=B3={x}` has pairwise credit `3 > S-|R|=2` but union credit only `2`. Exact max-flow analysis shows that all capacitated Hall cuts, not pairwise totals alone, are required; see `docs/failed-approaches.md`. |
| UNIF-03 | Prove LRC by factorwise absolute Fourier main-term domination. | rejected | `(1,2,3)` at pivot `3` has good residues `3,9`, but the filtered-bad factorization has `A-2T0=(4*sqrt(3)-5)/72>0`. Equivalent factorizations can give opposite outcomes, and `(4,5,9)` fails at every certified pivot; see `docs/fourier-pivot-badsets.md`. |
| UNIF-04 | Certify a pivot from total pair-intersection energy alone. | rejected | Sol Pro's proposed strict second-moment inequality fails at every pivot of `(1,2,5,7,9,11,12,13)`; neither the simple union bound nor `P < Phi(S,R)` holds. Exact per-pivot values are logged in `research/sol-pro/session-001.md`. |
| UNIF-05 | Prove uniform fiberwise balanced growth by crediting the best earlier parent in each child congruence fiber. | rejected | The sufficient criterion is Lean-checked and certifies most bounded residual tuples, including both hard eight-speed examples, but `(1,2,3,5)` fails at every pivot/order with optimum exactly `|R|`; `t=1/4` is nevertheless a witness. See `docs/fiberwise-hall-audit.md`. |
| UNIF-06 | Prove uniform additive fiber-credit growth without the common-`q` relaxation (`OPT-ADD-UNIF`). | rejected | The primitive tuple `G=(15,21,40,48,56,105,126,280,1200)` has optimized surplus `(0,-31,0,-32,-2,-2,-62,-48,-24)` in pivot order, so no pivot satisfies the required strict inequality. Two independent exact implementations reproduce the full table; a clean-room oracle enumerates all `8!` orders per pivot. This rejects only the additive-order sufficient condition. Pivot `48`, residue `39`, gives the direct lonely time `13/160`, kernel-checked in `OptAddCounterexample.lean`. See COMP-09. |
| UNIF-07 | Prove the ranked-fiber modular inequality `RF-UNIF`, or find a tuple failing it at every pivot. | rejected | `(2,3,7,9,10,12,15,16,19)` has ranked expected bound at least the candidate count at every pivot; exact scores are in `docs/ranked-fiber-averaging.md` and a regression test recomputes them. It is not an LRC or additive-certificate counterexample: pivot `3` has additive cost `25<27` and explicit witness `t=7/30`. Uniform random ordering can therefore miss a rare favorable order. |
| UNIF-08 | Prove uniformity from pairwise overlap energy alone. | rejected | The coarse random-order bound `sum |B_i| - (2/m) sum_{i<j}|B_i intersect B_j|` fails at every pivot of `(1,2,5,6,8)`, although the exact additive certificate succeeds at pivot `5` with bound `23<25`. Ranked within-fiber parent counts contain information lost by pairwise energy. |
| UNIF-09 | Prove or refute `GCD-CLOCK-UNIF`. | rejected | Sol Pro supplied `(8,15,35,40,48,56,63,75,78)`; a targeted independent search then found the simpler primitive failure `(6,8,15,21,28,35,40,48,75)`. Both have exact expected cost strictly greater than `9*A` at every pivot, and regressions recompute every rational margin. Neither is an additive or LRC counterexample: for the smaller tuple, the conditional-expectation greedy order at pivot `75` costs `643<675`. No global minimality of the smaller example is claimed. |
| UNIF-10 | Prove a state-local greedy ordering rule. | rejected | Minimum current increment, one-step lookahead, maximum raw credit, and maximum normalized credit each have an exact fixed-pivot failure where unrestricted additive optimization is strict. All admissible tie choices were optimized, so tie-breaking cannot repair the listed examples. Another pivot rescues every tuple in the documented small boxes; cross-pivot versions are unproved. |
| UNIF-11 | Prove that a bounded deliberately chosen prefix followed by GCD-clock conditional expectation succeeds at some pivot. | open | The one-prefix condition `min_i V_i < n*A` repairs the UNIF-09 counterexample at pivot `8`, even though its initial expectation fails everywhere. It fails at the other eight pivots of that tuple, and no cross-pivot uniform theorem is known. Longer prefixes interpolate toward the full optimized-additive endpoint, which is now rejected by UNIF-06; success on one counterexample is not evidence that any fixed prefix length is universal. |
| UNIF-12 | Prove two-sided relocation uniformity `RELOC-UNIF`. | open | Require a pivot where every order of cost at least `n*A` has a strictly cheaper one-item insertion neighbor. Exhaustive enumeration reproduces that every two-sided local minimum is strict on the RF, GCD-clock, two hard eight-speed, and `(1,2,3,5)` stress tuples. Left-only moves are insufficient: exact bad local minima occur on both nine-speed tuples. PART-25 proves only the generic descent implication; the arithmetic local-improvement assertion is open. |
| UNIF-13 | Prove or refute front-loaded `3-ANCHOR-UNIF`. | rejected | The primitive tuple `(10,37,45,51,54,56,61,71,91)` has minimum corrected three-anchor costs `92,333,407,461,492,504,553,645,827` against thresholds `90,333,405,459,486,504,549,639,819`. Equality at pivots `37` and `56` fails the strict condition. A clean-room literal oracle checked all 504 triples and direct enumeration found no one/two-anchor rescue. An adaptive full order still costs `84<90`, and `t=3/100` is a direct LRC witness. |
| UNIF-14 | Prove or refute `ANCHOR-STAR-UNIF`. | rejected | The exact primitive tuple `(8,15,35,40,48,56,68,75,78)` has negative best anchor-star margin at every pivot; the global best is `-32/21` at pivot `15`, anchor `75`. The smaller tuple `(1,4,5,7,8,9,10,11,17)` also fails, with an equality row of margin `0` that audits the required strict inequality. Both retain strict three-anchor certificates. |
| UNIF-15 | Prove or refute `DISPERSION-STAR` / residual-profile variation uniformity. | rejected | The exact primitive tuple `(8,15,35,40,48,56,68,75,78)` has strictly negative best pair-part-plus-`Delta` margin at every pivot; its global best is `-76/21`. Literal and optimized all-row implementations agree. The proved local inequality `Gamma >= Delta` remains valid; only its proposed cross-pivot uniform consequence is false. |
| UNIF-16 | Prove uniformity from first/second profile moments only. | rejected | The maximum of the integer-balancing, bounded-energy, and Cauchy-energy cell bounds fails at every pivot of `(8,15,35,40,48,56,63,75,78)`; its best row has margin `-2/3`, while full `Delta` gives `+4/21`. Profiles `(0,1,3,3)` and `(1,1,1,4)` have the same first two moments but different `L1` dispersion, confirming intrinsic information loss. This rejects only the profile-free moment closure. |
| UNIF-17 | Prove or refute `RANGE-SUM-STAR-UNIF`. | rejected | The exact primitive tuple `(8,15,35,40,48,56,68,75,78)` has strictly negative best pair-part-plus-`W` margin at every pivot; its global best is `-76/21`. Two additional primitive examples attain global margin exactly `0`, confirming that the strict boundary cannot be relaxed. The profile majorization remains valid; its uniform sufficient condition is false. |
| UNIF-18 | Prove or refute `TOP-CYCLE-UNIF`. | rejected | The primitive tuple `(5,28,35,40,68,88,108,148,165)` has top-only cost minus threshold `(0,6,0,2,12,18,10,32,4)` at its nine pivots, so no row is strict. Two independent literal reconstructions reproduce every `S,F_top,tau_top,D_top` value; a clean-room run exhaustively checks all `8!` orders at every pivot. This rejects only the top-parent sufficient condition. At pivot `28`, unrestricted optimum is `250<252`, and `r=6` (`t=3/140`) is a direct lonely witness. See `docs/top-cycle-failure-audit.md`. |
| UNIF-19 | Prove or refute three-step chain uniformity `CHAIN3-UNIF`. | rejected | The primitive tuple `(8,15,35,40,48,56,75,132,147)` has `B_3-beta=(2,1,6,16,50,24,0,96,72)` in pivot order, so no pivot satisfies the required strict inequality. Two independent exact implementations reproduce every residue, token, nested-chain component, and optimum; equality at pivot `75` is failure because bad-set avoidance is strict. This rejects only the three-step sufficient condition: unrestricted additive ordering succeeds at pivots `15`, `35`, and `75`, while `r=13` at pivot `8` gives the direct lonely time `13/80`. See `docs/chain3-counterexample-audit.md`. |
| PACK-08 | Prove the exempt-owner coloring characterization of whole-block packing. | proved-math | Fiber uniqueness and capacity `mu(r)-1` give both directions exactly. This characterizes the packing optimum, not pivot noncoverage itself. Lean formalization remains optional until a uniform arithmetic use is found. |
| CORR-01 | Prove the alternating-binomial zero-indicator bound without unsafe division. | proved-lean | `CorrelationPolynomial.lean` proves the denominator-cleared identity for `d>0`, `1<=k<=n-1`, its nonpositivity, the separate `k=0` value, and the rational indicator inequality. It deliberately makes no modular or uniform assertion. |
| CORR-02 | Verify the common-LCM slice identity and Response 39 arithmetic. | proved-math | A clean-room exact verifier reproduces `G`'s full weighted histogram, moments, safe counts, `L_(9,2)=3305/2`, all T10/T11 rows and witnesses, and the near-top boundary clarification. The full histogram is audit evidence only because it already contains `D_0`. |
| CORR-03 | Derive fixed-order moments `H_q` directly from CRT intersections without enumerating safe residues or the full `k` histogram. | proved-math | The generalized-CRT subset formula includes candidate-slice multiplicity, strict endpoints, self/empty cases, repeated numerical speeds, compatibility, and candidate subtraction. Its primary computation obtains `G`'s `H_0,...,H_4` without `D_0` or safe enumeration; an independent comparator and adversarial review agree. `WeightedZeroCount.lean` separately kernel-checks the abstract double count and guarded weighted inequality. |
| CORR-04 | Prove a uniformly positive correlation bound at fixed or prescribed sub-tautological depth. | rejected | For `A_n=(1,...,n)`, `L_(n,d)<=n*phi(n+1)-2*C(n-1,2d+1)/(n-1)`. Every fixed depth is negative on infinitely many `n`, and `d=(n-3)/4` fails for every `n congruent to 59 mod 60`, although `t=1/(n+1)` is lonely. This rejects those depth rules, not arbitrary adaptive depth or other moment polynomials. |
| CORR-05 | Find a growing-order cross-pivot or adaptive genuinely sub-tautological correlation certificate not defeated by the consecutive family. | open | Maximal depth is exact inclusion--exclusion. Optimizing all coefficients still fails for every fixed order, so a surviving theorem must let order grow with `n` or exploit additional CRT intersection-table structure discarded by numerical moments. |
| CORR-06 | Optimize exactly over every feasible fixed-order zero-indicator polynomial and determine whether coefficient choice repairs uniformity. | rejected | The exact primal/dual and vertex classification give stronger finite certificates, including degree 3 on `G`. However, for every fixed order `r`, infinitely many consecutive tuples have exact optimized value zero despite `t=1/(n+1)`. This rejects uniform fixed order only. The abstract hockey-stick and cleared histogram bound are proved in `ConsecutiveCorrelationObstruction.lean`; arithmetic realization and low-totient estimates remain proved-math. |
| CORR-07 | Determine the minimum successful Response 39 depth on the consecutive tight family. | proved-math | For every `n>=84`, `d_min=floor(n/2)-2` for even `n`; for odd `n` it is that value exactly when `3*n*phi(n+1)>(n-2)(n-3)`, otherwise `floor(n/2)-1`. Both cutoff gaps occur infinitely often. The sharpened five-residue depth-one bound is symbolic from `n=84`; exact scans audit smaller values. This classifies one certificate and does not prove LRC. |
| CORR-08 | Construct and audit a sublinear-degree feasible polynomial on consecutive speeds. | proved-math | The shifted/scaled Chebyshev polynomial has rational degree `O(sqrt(n) log n)`, equals one at zero, and is uniformly slightly negative at positive multiplicities. `ChebyshevMoment.lean` kernel-checks the rational recurrence/degree, `[-1,1]` bound, affine-grid feasibility, abstract score inequality, and composed positivity theorem under an explicit growth premise. The analytic logarithmic-degree growth estimate remains on paper. The proof uses the already-known safe mass `D_0=n*phi(n+1)`, so it is not a noncircular certificate proof. |
| CORR-09 | Evaluate the general Chebyshev pivot score noncircularly and force it positive uniformly. | open | The exact forward-Newton identity and direct generalized-CRT evaluation use only `H_0,...,H_min(r,n-1)` and avoid `D_0` and a full histogram. Beyond Response 42's original boxes and 4,276 mutations, exact scans now find no all-pivot score failure in 275,494 targeted executions, 190,488 Prompt 44 adversarial tuples, and 672,697 independent two-/three-coordinate mutations. These are finite certificate tests only. They do not supply the missing uniform CRT-table inequality, and zero failures does not imply positivity of a global sum or LRC. |
| CORR-10 | Derive and test a low-order sufficient inequality from the Chebyshev score. | rejected | Response 44's pointwise quadratic minorant is sound, but its proposed uniform use fails. For `(1,3,5,6,7)`, the exact scaled quadratic bounds are `(-14722,0,-324,-486,-972)` at the five pivots. Three full scores remain positive and `t=7/30` is lonely, so this rejects only the quadratic sufficient condition. Exact lower-dimensional calibration is documented without claiming global minimality. |
| PART-47 | Prove the two-hole bounded-height theorem. | proved-lean | `BoundedHeight.lean` proves interval selection, inverse construction, exclusion of residues `0,+/-1`, the reusable `q<=2N` denominator lemma, all four hole cases, and extraction of exactly two holes from an injective family. `boundedHeight_stationary_witness` gives every positive injective `n>=3` family of maximum at most `n+2` a closed-boundary witness; earlier base cases cover smaller dimensions. Thus an integer counterexample would have maximum at least `n+3`. The exact finite audit checks 1,530 cases but is not used by the proof. |
| PART-48 | Prove the four-hole max-speed `n+4` theorem. | proved-math | For `N>=12`, the interval `[N+c+4,2N]`, a six-class totient argument, the exact classification `phi(c)<=6`, and a checked exceptional table force a reciprocal or inverse witness. Independent adversarial review accepted the proof and regenerated every table row. `N=4..11` are covered by an exact 2,982-case certificate; `N=2,3` are deductive/base cases. Hence an integer counterexample would have maximum at least `n+5`. A conditional Lean bridge suffered nonterminating elaboration and was removed; formalizing the large-`N` runner bridge, arithmetic selector, and small certificate remains open. |
| PART-49 | Prove the five-hole max-speed `n+5` theorem. | proved-math | For `N>=20`, the interval `[N+c+5,2N]`, an eight-class complement bound, the exact classification `phi(c)<=8`, and a checked 70-row exception table force a reciprocal or inverse witness. An independent reviewer reconstructed the proof and table. A separate clean-room C++ implementation reproduced all 134,568 exact configurations for `N=4..19`, including three direct small witnesses; `N=2,3` are base cases. Thus an integer counterexample would have maximum at least `n+6`. This computer-assisted theorem is not yet one Lean declaration. |
| PART-50 | Prove an unbounded constructive height gain. | proved-math | For positive integers `N,t`, `16*t^2+t<=N` implies every `(N-1)`-speed positive-integer set bounded by `N+t` is lonely. The proof combines the same denominator interval with `phi(c)>=sqrt(c/2)`. Independent review checked nonemptiness, all endpoints, and the totient inequality. With PART-49, `T_N=max(4,floor((sqrt(64N+1)-1)/32))` gives the counterexample restriction `max A>=N+T_N+1`. Formalization in Lean remains open. |
| PART-51 | Prove an elementary logarithmic constructive height gain. | proved-lean | `logarithmicHeight_family_witness` proves that if `(4*(Nat.log 2 N+1)+1)*t<=N`, every injective positive `n=N-1` integer family bounded by `N+t` has a real witness at the closed `1/N` threshold. The proof includes the reciprocal and coprime-denominator branches. Exact audits cover 553,106 arithmetic cases and 361,220 hole configurations. Combining mathematically with PART-49 gives `T_N=max(4,N div (4*(Nat.log 2 N+1)+1))` and `max A>=N+T_N+1` for any integer counterexample. |
| PART-52 | Formalize the logarithmic-height theorem in Lean. | proved-lean | `LogarithmicHeight.lean` proves the unconditional sharp product `c<=(omega(c)+1)*phi(c)`, `omega(c)<=Nat.log 2 c`, complete-period and short-interval coprime selectors, missing-height extraction, reciprocal divisibility exclusion, and final inverse-residue assembly. `logarithmicHeight_stationary_witness` exposes `N=n+1`; `logarithmicHeightGain_stationary_witness` proves the explicit division gain; `logarithmicHeightGain_positiveInteger_witness` uses exactly the canonical `UnitCircle` norm. Axiom probes report only `propext`, `Classical.choice`, and `Quot.sound`. |
| PART-53 | Prove the exact primorial-ratio bounded-height theorem. | proved-lean | If `P_N` is the largest primorial at most `N` and `Q_N=phi(P_N)=product_(p divides P_N)(p-1)`, then `boundedPrimorial_ratio_maximum` kernel-checks `max_{1<=c<=N} c/phi(c)=P_N/Q_N` in cross-multiplied natural arithmetic, including attainment at `c=P_N`. The proof replaces a maximal prime factor by a missing smaller prime, strictly decreases the radical, and uses strong induction plus Euler's exact product. `boundedPrimorialHeight_family_witness` then proves the common closed `1/N` witness under `(4*P_N-Q_N)*t<N*Q_N`; `boundedPrimorialHeightGain_family_witness` proves the exact maximal gain `floor((N*Q_N-1)/(4*P_N-Q_N))`. The older general ratio-premise interface is retained separately. Axiom probes report only `propext`, `Classical.choice`, and `Quot.sound`. This is a bounded-height theorem, not unrestricted LRC. See `docs/response45-audit.md`. |
| PART-54 | Reconstruct Kanold's Jacobsthal bound and derive the linear-height theorem. | proved-lean | `KanoldHeight.lean` defines the exact proposition `KanoldIntervalBound`: every `Finset.Ico start (start+2^omega(c))` contains a number coprime to positive `c`. `KanoldVandermonde.lean` proves it and `seventeenThirdsHeight_family_witness` consequently proves that `17*t<=3*N` and `max A<=N+t` force one common closed `1/N` witness for every positive injective `N-1` speed family. Lean also discharges `5*2^omega(c)<=2*c` for every `c>=7`, all `c<=6` interval arithmetic, every natural-subtraction guard, and `start+ell=2*N+1`, so `q=2*N` is included. Axiom probes use only standard foundations. The arbitrary fixed-hole coefficient-five selector fails at `(11,2,6)`; Response 47 repairs the family argument on paper, and formalizing that improvement is tracked separately in PART-57. |
| PART-55 | Prove the Kanold interval bound by an independent finite-algebra argument. | proved-lean | `kanoldIntervalBound_vandermonde` expands `product_{p|c}(1-zeta_p^x)` into `2^omega(c)` pairwise-distinct exponential modes. `subsetExponent_mod_injective` isolates a symmetric-difference prime; `primitiveRoot_subset_nodes_injective` separates the complex nodes; `roots_product_expansion` and `roots_product_eq_zero_of_dvd` verify the filter; and `exponential_sum_not_vanish_consecutively` applies Mathlib's Vandermonde kernel. Vanishing on the full half-open interval would force the nonzero empty-subset coefficient to vanish. `coprime_of_avoids_primeFactors` closes the exact `Nat.Coprime` statement, including `c=1` and `start=0`. See `docs/response46-audit.md` and manuscript Section 37. |
| PART-56 | Formalize the square-root-log totient fallback. | proved-math | Response 46 proves `c^2<=8*omega(c)*phi(c)^2` for `c>=2` by ordering prime factors, the slot bound `p_i>=2*i-1`, and a Wallis-product recurrence. Independent algebra audit found no gap. Scratch Lean already compiles the slot bound, recurrence, and final square extraction; the open formal bridge is the complete Euler-product comparison over the ordered distinct prime factors. This route is weaker than the Kanold height theorem but avoids complex roots and matrices. |
| PART-57 | Improve the Kanold bounded-height coefficient from `17/3` to `5`. | proved-lean | Under `5*t<=N`, `five_short_interval_exception_classification` proves that failure of the arbitrary-hole Kanold interval length occurs exactly at `(N,t,c)=(5,1,3),(10,2,6),(11,2,6)`. `exists_second_missing_of_extra_speed` formalizes the injective-cardinality repair; the middle case uses `q=19`; and `fiveHeight_family_witness` assembles the unconditional common closed `1/N` witness. Axiom probes report only `propext`, `Classical.choice`, and `Quot.sound`. The recovered verifier independently checks 36,601,650 admissible triples through `N=1000`. See `docs/response47-audit.md` and manuscript Section 38. |
| PART-58 | Formalize the fastest-pivot ratio criterion and its min/max obstruction. | proved-lean | `FastestPivot.lean` proves that if `N>=2`, `B` is a positive maximum speed, and `B<=(N-1)*s` for every speed `s`, then the explicit pivot numerator `r=N-1` is a candidate and avoids every strict bad set. `fastestPivot_family_witness` gives the resulting common closed `1/N` witness. By contraposition, `slowest_fastest_gap_of_no_fastestPivotCertificate` proves that failure of every certificate at the fastest pivot forces `(N-1)*a_min<a_max`. This restricts a failed fastest-pivot or top-two strategy; it does not prove that either strategy always succeeds. |
| PART-59 | Improve the Kanold bounded-height coefficient from `5` to `4`. | proved-lean | `five_mul_two_pow_omega_le_of_40_le` controls every `c>=40`; `four_short_interval_exception_classification` kernel-checks the exact remaining twelve triples. Nine explicit coprime denominators close nine families. At `(4,1,2)`, `(11,2,6)`, and `(20,5,12)`, the moduli `7`, `19`, and `37` have complementary holes `N+t`; if that height occurs, `exists_second_missing_of_extra_speed` supplies a different missing value and the classification cannot repeat the same exception. `fourHeight_family_witness` assembles the common closed `1/N` witness under `4*t<=N`. A fresh adversarial audit reproduced the exception list, endpoints, and all repairs and returned `ACCEPT`. This remains a bounded-height theorem. See manuscript Section 40. |
| PART-60 | Improve the bounded-height coefficient from `4` to `3`. | proved-lean | `three_short_interval_large_or_exception` proves that, outside five exact small triples, every short missing height satisfies `N<2*c`. Under a hypothetical failure of the full common-witness existential, `threeHeight_family_witness` shows that every missing `c` in `[1,N]` forces the occupied extra speed `2*c`. For the missing-base set `M` and occupied-extra set `E`, Lean proves `|M|=|E|+1`; injectivity of `c |-> 2*c` and `2*M subset E` give the contradiction. Explicit cardinality and complementary-hole arguments repair `(N,t)=(6,2),(12,4),(13,4)`, while `(4,1)` and `(12,3)` use PART-59. The exact source compiled standalone, an isolated target build succeeded, targeted probes use only standard axioms, and independent adversarial review returned `ACCEPT`; the authoritative clean full-project replay is still pending. This is a bounded-height theorem under `3*t<=N`, not unrestricted LRC. See manuscript Section 41A. |
| MIN-01 | State and exploit the corrected minimal-counterexample residual class. | open | The top comparison `a_n<n*a_(n-1)` is valid only after choosing a least counterexample dimension, or under explicit `LRC(n-1)`, followed by fixed-dimensional sum minimality. Divisor cover plus deletion-gcd-one further gives `1<=#{i:d|a_i}<=n-2` for every `2<=d<=N`. These restrictions, the improved `max>=N+T_N+1` height bound, lcm/product bounds, scaling, and signed-descent irreducibility remain compatible: the infinite family `U_N=(3,4,...,N,N+4)` satisfies their finite-height versions and is explicitly lonely at `1/(3N)`. A new total-pivot input is still required. |
| PART-46 | Prove the small-denominator witness and coprime cross-pivot scaling theorems. | proved-lean | `SmallDenominatorWitness.lean` proves that a modulus `2<=q<=N` dividing no speed gives the witness `t=1/q` and derives divisor-cover necessity by contraposition. `CrossPivotScaling.lean` proves `rho_(qM)(qx)=q*rho_M(x)`, coprime candidate preservation, componentwise strict-bad equivalence, an injection of safe candidate grids, and cardinal monotonicity. Axiom probes use only standard foundations. |
| DESC-07 | Prove the signed-divisor certificate transport and exploit it uniformly. | proved-math | The one-directional transport is audited, including `(1,2,3,24)->(1,2,3,6)` and the changed-pivot map `r->q*r`. The uniform exploitation remains open: `G` has no eligible replacement, so a minimal-counterexample theorem must force descent or characterize irreducibles. |
| DESC-08 | Characterize largest-speed signed descent in the comparable regime. | proved-lean | `ComparableSignedDivisor.lean` proves the forced top equation, quotient bounds, exact necessity and sufficiency of the lower-pivot divisor alternatives, ordering and divisibility of the replacement, and strict total-sum decrease. Instantiating the indexed lower family and composing with signed-certificate transport/pivot completeness remain explicit bridge obligations. The result gives only a necessary irreducibility condition, not a contradiction. |
| COMP-11 | Prove or refute the top-two pivot strengthening. | open | For sorted positive integer speeds, restrict the exact pivot existential to the two numerically largest speeds. This would imply the unrestricted pivot certificate and hence full LRC through the Lean-verified equivalences, but only finite evidence is known. `audit_top_two_pivot_search.cpp` checks 878,245 primitive tuples in six exact boxes, CRT divisor-cover subdomains, 640 scaling identities, and 86,745 structured mutations through 15 runners with no top-two failure. The endpoint restriction is already false at `(1,4,5,6,7,11)`. See `docs/top-two-pivot-search.md`. |
| COMP-12 | Prove or refute the fractional two-grid dual invariant. | rejected | The global invariant is false at `(n,N,B,A)=(6,7,98,187)`: an exact fractional lower-speed cover has mass `962/241<4`, and every one of the 1,224 mutually-top-good vertices receives cover mass at least `248/241`; weak duality therefore rules out the required vertex-dual mass greater than `n-2=4`. Exhaustive enumeration of all 3,464,840 four-speed subsets finds no integral cover (best `1142/1224`), so this rejects only the fractional-dual proof invariant. The integral top-two pivot conjecture remains open. The earlier `(7,8,14,16)` successful fixed dual remains valid. See `docs/top-two-fractional-obstruction.md` and `docs/top-two-fractional-dual.md`. |
| COMP-13 | Prove the top-two pivot property for the saturated lower interval. | proved-lean | For every `N>=4` and `B>N-2`, `saturatedTopTwo_avoids_pivotBadResidues` gives a canonical certificate for `(1,2,...,N-2,B)` at pivot `N-2` or `B`. If `N∤B`, it uses pivot `N-2` and numerator `N-2`; if `B=N*k` but `(N-2)∤k`, it uses the same pivot and numerator `N-1`; if `B=N*(N-2)*k`, it uses pivot `B` and numerator `(N-1)*N*k-1`. Candidate membership, every lower speed, the top speed, and the closed boundary are Lean-verified. This proves only the saturated class, not the unrestricted top-two conjecture. |
| COMP-14 | Prove the fastest-pivot interval-compression criterion. | proved-lean / proved-math | `pivot_le_cyclicResidueDistance_mul_of_band` and `exists_fastestPivotCertificate_of_extremal_band` kernel-check the certificate core: for an explicit lower speed and positive upper bound `A`, any `r` with `N∤r`, `B<=r*a_lower`, `r*A<=(N-1)*B`, and every nonpivot speed in that band is a fastest-pivot certificate. The first draft failed because `.trans` selected a non-strict relation; the exact `lt_of_le_of_lt` repair compiled in ext4 and its probes use only standard axioms. For ordered `0<a_1<...<A<B`, the manuscript corollary with `L=ceil(B/a_1)` and `U=floor((N-1)*B/A)` shows failure forces `U<L` or `U=L` with `N|L`; this ceiling/floor corollary remains `proved-math`. Full clean integration replay is pending. See `docs/response49-audit.md`. |
| COMP-15 | Prove and exploit the common top-grid slice. | proved-math | Writing `A=g*alpha`, `B=g*beta`, `g=gcd(A,B)`, the residues `r_A=alpha*u` and `r_B=beta*u` represent the common time `u/(N*g)` whenever `0<=u<N*g`, `N∤alpha*u`, and `N∤beta*u`. The slice has exact size `g*(N-gcd(N,alpha)-gcd(N,beta)+1)`. Exact generalized-CRT counts for lower bad subsets give a sound strict union-bound criterion. This is sufficient only and can be vacuous: at `(N,A,B)=(7,98,187)` the common slice is empty. See `docs/response49-audit.md`. |
| COMP-16 | Extend the saturated three-residue proof unchanged across one lower deletion and one top insertion. | refuted | The transplanted second case fails on `(2,3,4,5)` at `N=5`: pivot `4`, residue `4` sends speed `5` to cyclic distance zero. The transplanted fast case fails on `(1,3,4,20)`: pivot `20`, residue `19` has distance `19<20` at speed `1`. Deleting speed `1` restores that particular residue, exposing the missing minimum-speed inequality rather than a top-two failure. See `docs/failed-approaches.md`. |
| COMP-17 | Close the global top-two conjecture from interval compression or a cross-pivot slice. | conjectural | B1 leaves the residual large-gap/compressed-interval regime without control of lower residues modulo `N*A`. B2 is silent when its common slice is empty. Coarse top-speed divisibility cannot choose a pivot: the exact `N=7` tuples `(1,4,5,6,7,11)` and `(1,3,4,5,7,18)` have matching basic top divisibility flags but require opposite top pivots. A finer signed cross-pivot or integral-cover theorem remains necessary. No unrestricted theorem follows. |
| FULL-01 | Prove or disprove the conjecture for all dimensions. | open | The recognized mathematical problem remains open. |
| AUD-01 | Independently audit every nonstandard imported theorem and all axioms. | open | `AxiomAudit.lean` now also covers the exact residue equivalences, boundary witness, canonical pivot reduction, and global positive-integer certificate equivalence; every probe reports only `propext`, `Classical.choice`, and `Quot.sound`. An independent agent found no quantifier, endpoint, nonemptiness, or dependency gap. The open status reflects that no final all-dimensional proof/import audit exists. |
| SOL-01 | Run iterative GPT-5.6 Sol Pro research/rebuttal loop in a separate Chat conversation. | proved-math | Session 001 records the signed-in GPT-5.6 Pro dialogue, exact objections, failed Lean goals, repairs, and independently audited counterexamples. Response 46 supplied the accepted finite-algebra/Vandermonde proof of Kanold's bound, sharpened the height arithmetic to `17*t<=3*N`, and supplied a sound square-root-log fallback. Its recovered verifier and output reproduce exactly. The Response 45 primorial maximizer and Response 46 Kanold proof are now formalized independently in Lean. Finding a full pivot bridge remains open; no proof or disproof of unrestricted LRC resulted. |

No row may be promoted to `proved-lean` solely because a weak or incorrectly
quantified surrogate theorem compiles.
