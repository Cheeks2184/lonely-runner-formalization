# First uniform mechanism screens after canonical six runners

The canonical six-runner checkpoint merged through PR57 at
`76d51525dddaac5613e1bb613b7bb9dc7605dabe`. Both required CI jobs passed;
root verified the reviewed head's ancestry and matching local/remote main.
Research now concerns arbitrary N, as required by the project instructions.
No successive runner-count milestone is planned.

Two proposed shortcuts fail at independently reviewed mathematical scope.
First, deletion gcds, divisor covers, reciprocal-orbit capacity inequalities,
the sharper published height cutoff and the elementary fast-speed exclusion
are simultaneously satisfied by consecutive blocks starting at lcm(2,...,N),
for every N>=5. An explicit common witness shows why those necessary arithmetic
conditions cannot by themselves force a contradiction.

Second, a prime-blocker construction defeats all proposed short dilations
2<=alpha<N and all undilated shifts whose actual complement phases would permit
forward improvement. It has genuine common-time integer speeds, the basic
divisor profile and a local constrained maximum for every N>=9. The small N9
example fails the stronger orbit-capacity screen, a distinction discovered in
review. The root addendum proves that the same factorial construction satisfies
all those stronger inequalities and the inclusive MSS height bound for every
N>=28. Therefore that strengthened local rescue lemma is still false.

This does not refute the actual global constrained-maximum argument. The small
example has a separate full witness and an improving dilation alpha=11, outside
the rejected slope range. The general family is not asserted to satisfy a
missing-witness hypothesis. No LRC counterexample has been produced.

Both author reports and the root addendum received an independent Astra Extra
High review. The reviewer replayed the exact retained integer/rational controls;
both passed, and all 16 source bindings matched. Root read the complete proofs
and review, and checked report and receipt hashes. These are manuscript results
with reproducible controls, not new Lean theorems. Lean sources did not change,
so an unchanged full local build was not repeated. Full receipts are bound in
`research/astra-pass59-verification.json`.

The next screen asks whether some deletion's own-margin seed has a guaranteed
safe interval reaching a full witness. It permits choosing any pivot, which
separates it from the previously refuted fastest-deletion strategy. Its exact
all-real interval geometry is under investigation; finite positive controls
will not be treated as a uniform proof. Straightforward implementation remains
Terra High work once a useful contract is settled. The unrestricted formal
proof/disproof goal remains active and unresolved.
