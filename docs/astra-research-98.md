# Pass 98 checkpoint

PR #96 merged `f34b3bb` from `afebb4b`. Recorded publication evidence verifies 3,731 hosted jobs, 710 reports, both required checks, and 30 remote hashes plus ancestry. Pass 98 is a checkpoint pass; no subsequent PR number or hosted CI result is claimed here. It adds no Lean declaration, kernel result, or proof of the unrestricted Lonely Runner Conjecture.

The accepted positive-candidate escape-cycle manuscript, `research/astra-positive-candidate-escape-cycles.md`, and its independent review retain the full deletion set and exact integer carries. Varying parent labels and signs prevent the proposed cycle cancellation. A stronger seed for the tuple obtained by deleting the maximum m and a source parent a and inserting the candidate can lie in W_m, in W_a, or outside every original deletion set when both removed labels are bad. The native-good controls refute automatic cancellation and seed transfer; they do not satisfy canonical strict failure.

The accepted mixed-sink manuscript, `research/astra-mixed-candidate-zero-sinks.md`, and its independent review concern an incoming candidate u at a zero-only region with complementary parents c+d=m. Only when u is bad at the center do they give uZ >= pA and (u+p)/G >= N+1, where s=min(c,d), p is the parent selected by the sign of the central candidate residue, G=gcd(m,c,u), Z=ND-m, and A=m-N*rho for central residue magnitude rho. The corresponding bound is mu >= [alpha*m+pG/(u+p)]/(m+s). Candidates already good at the center remain unresolved. The exact native-good control refutes automatic parent reduction while leaving full-failure transport unproved.

The next active investigation is same-parity pair descent, not a count ladder. No local build or cache restoration was needed.
