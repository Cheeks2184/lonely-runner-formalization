# Pass 56: one-even branch and remaining sixth-phase certificate data

One fixed exact-data run evaluated the reviewed Renault 6.1/6.4 candidate
families. The two mixed-coordinate core tables each have 42 rows; the closed
safe-domain table has 26. The generator reported 6,975 band predicates,
19,665 containment tests, and 41,202 common-bit witnesses, with a zero exit.
Its source, actual exit, full streams, timing and output hashes are retained.
Independent rational verification passed all 6,975 band masks, 321 supplied
dominations and 41,202 witnesses. Root fully read the report and checked its
seven current bindings and raw streams without another run. Core minimality,
least-dominator tie choice and the containment counter remain generator-reported;
none is required for certificate soundness. Lean kernel checks are still needed.
This finite-data result is not a phase theorem.

Before execution, root source review corrected the doubled singleton numerator,
the differing half-open/closed atom counts, and successful-exit handling.
The one authorized numerical pass then succeeded without a retry. Root made
no generator source edits. Its 0.17-second reported execution duration is not
worker task duration or token accounting; those values remain unavailable.

The one-even parity assembly has a complete source-reviewed implementation
plan using the now-checked signed and reflected phase choices. Its contracts
permit repeated positive speeds and require no primitive gcd assumption.
Intermediate times are arbitrary real values; the maximizer contradiction
uses its all-real scope and restores positive time only in the final wrapper.
The one-even module compiled after two Terra High repair rounds. Root fully
read its source and accepted both exact contracts, checked the before-source
and raw-log bindings, and verified the final source/artifact hashes. Independent implementation review accepted both exact contracts. Root checked
all 23 current source/artifact guards and the complete successful type/axiom
streams without repeating the same probe.

Canonical six total runners and unrestricted LRC remain unresolved. The
established next research focus after verified canonical N=6 is a uniform
arbitrary-N mechanism.

The preceding checkpoint merged through PR54 at
`a4e324ac4fdd8804840dc9563b4d69d923c07b08` after both required checks passed.
Root verified the exact reviewed head is an ancestor of origin/main.

A shorter assembly for the two weak sixth-shift helpers uses checked common
third shifts and half-turn norm identities. The weak-even case chooses k or
k+3 for a common nonzero third shift; the weak-odd case maps common third
shifts 0,1,2 to sixth shifts 3,4,2. The weak-shift module compiled after one worker repair. Root accepted the full
source and matched final artifacts and raw logs. Its failed-source snapshots
were not retained, contrary to the delivery protocol; hashes and diagnostics
remain, and the gap is disclosed. Independent source review accepted both exact contracts; root matched all 27
guards and the successful complete type/axiom receipt without a repeated probe.

The fixed ordinary-kernel pilot proved five finite rows on its first invocation
in 2.992 seconds, with each declaration reporting only `propext`. Its complete
before-source snapshot and compiler streams match the saved hashes. The pilot
does not prove all rows or establish an all-row runtime estimate. Two frozen
pilot comments incorrectly call 6.1 atom 119 a singleton: the actual checked
propositions use the odd open interval. The 6.4 atom 80 is the final singleton
at 5/6. Full table transcription uses the correct domain descriptions.

The integrated source build passed 3,719 jobs. The trust audit accepted 659 theorem reports with only the permitted axioms.
The next finite-data task is a bounded full kernel proof of the band rows,
dominations and common-bit facts. In parallel, Astra is preparing exact real
phase assembly and the three-even parity proof from the compiled helper APIs.
These tasks support the remaining six-runner theorem; the unrestricted goal
and subsequent uniform-N priority remain unchanged.
