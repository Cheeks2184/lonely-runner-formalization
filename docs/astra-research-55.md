# Pass 55: signed phase choices and prescribed rational orbits

The signed and reflected versions of Renault 5.1 now retain one common affine
choice for all three phases, with the original closed dilation alternative and
strict shift alternatives. Reflection replaces b by 6-b in both branches;
independent signs are restricted to ±1. The corrected source preserves every
endpoint and the full arbitrary-real phase domain.

The rational-orbit helper attains any prescribed residue for a coprime
coefficient, including modulus one and target zero. Its application reduces an
arbitrary denominator q coprime to six by gcd(q,A). If q does not divide A, it
finds a fractional phase in the closed interval [1/12,1/6] or exactly 1/5.
Composite denominators and noncoprime original coefficients remain covered.

Three generic certificate interfaces connect exact natural arithmetic to real
affine sixth-phase bounds, cover closed cut intervals including their last
singleton, and select one common candidate across coordinate-indexed rows.
Strict singleton inequalities and closed interval-endpoint inequalities are
kept separate. These interfaces contain no concrete Renault 6.1 or 6.4 data.

The signed proof required a corrected closed-branch reflection and elaboration
repairs. Terra High compiled the orbit theorem after two worker repair rounds
and the generic interfaces after one. Complete raw failure and success logs are
retained. The orbit delivery did not retain failed source snapshots; its precheck
hashes and final source/artifact binding are recorded without reconstructing
missing history. The generic delivery retains exact source bytes before both
checks. No identical focused checks were repeated by root.

The signed/orbit independent review accepted four exact contracts, and its
single bounded import probe checked their full types and allowed axiom lists.
Root read the report and verified all 26 current guards and five retained probe
files. Generic-interface independent review and checkpoint integration evidence
are recorded in research/astra-pass55-verification.json when complete.

The next finite-data experiment targets the exact remaining Renault 6.1/6.4
contracts using the existing cut grid. Its assessment fixes candidate families,
strictness, domains, limits and independent verification requirements. An
assessment or generated finite table is not a kernel proof of those contracts.
The one-three-divisible branch, canonical N=6 and unrestricted LRC remain open
in this development. Once canonical N=6 is verified, research moves to a uniform
arbitrary-N mechanism rather than successive runner counts.

Pass 54 and the Terra High workflow changes merged in PR53 at main commit
a1d90c2ace37dc7779dc78e104da331965af6646. Both required checks passed, the
exact reviewed head was merged, and root verified origin/main ancestry.

The integrated source build passed 3,717 jobs. The trust audit accepted 655
theorem reports, including all seven new public declarations.

Independent review also accepted all three generic interfaces. Root fully read
that report and matched its 20 frozen guards and five retained probe files; the
single type/axiom probe passed. No duplicate probe was run.
