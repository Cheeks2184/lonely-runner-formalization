# Pass 57: remaining finite phase facts and real assembly

The fixed 6.1/6.4 data have full ordinary-kernel finite proofs. These cover all
three affine band-fact families, core-to-atom dominations, mask bounds, core
representatives and common intersections. Root matched the literal vectors to
the independently verified metadata and checked all generated row indices.
The safe intersection proof permits an arbitrary first core index, extending
the index coverage of the earlier fully sorted supplied-data witness stream.

Two operational attempts failed before source elaboration: Lake rejected a
compiler-only flag, then the Lake-generated compiler command failed at thread
startup under the memory cap. The supported direct Lean invocation with explicit
thread and stack options passed unchanged: Data in 2.296 seconds and Facts in
71.884 seconds, each under 8 GiB / 180 seconds. Root checked every physical before
snapshot, raw stream hash and final artifact. No source repairs or duplicate
successful compilation occurred. One root recovery-guidance round is recorded;
worker duration and actual token use are unknown.

The source-derived real assembly plan preserves half-open [0,1) mixed coverage,
closed [1/6,5/6] safe coverage and one shared candidate. It transfers fractional
phases by integer periodicity; it does not fold inputs to circle norms before
shifting. Root fully read it and matched 22 bindings. The two all-real public phase theorems now compile after one mechanical repair.
Root fully read the source and matched both attempt snapshots, raw streams
and final artifacts. Independent finite and real source reviews accepted the exact contracts.
The three signed adapters compiled after two worker repairs; root matched all
three physical before-source snapshots, complete streams and final artifacts.
A separate independent source review accepted their exact shared-choice, sign
and endpoint semantics without another Lean probe.

The previous checkpoint merged through PR55 at
830a62697cedba165fc44172d25614ee506d86ec after both required checks passed.
Root verified exact-head ancestry and synchronized main.

Canonical six total runners and unrestricted LRC remain unresolved. The exact
final assembly plan identifies the remaining parity, primitive normalization and
real-speed bridge obligations. After verified canonical N=6, the standing focus
is a uniform arbitrary-N mechanism rather than successive runner counts.

The full integrated build passed 3,723 jobs, and the trust audit accepted 683
theorem reports using only the permitted axioms. All 24 new finite, real and
signed public declarations are included in the explicit audit. The first-ten
Terra High delivery review retains the current division of labor; it records
less observed root source repair but no measured token savings.
