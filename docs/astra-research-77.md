# Pass 77: exact obstructions to closest-odd fold transport

This pass tests the missing reverse transfer in the proposed uniform
closest-odd folding argument. It does not prove or disprove unrestricted
LRC, and it does not start a sequence of higher runner-count proofs.

The rank-three coordinate analysis gives exact LRC-good controls with
index `C=2`, both saturated nearest-point planes, and a real direction
of strict improvement, but no improving parity-admissible integer point.
At every fixed moving dimension at least five, its real improvement
interval and depth can tend to zero with increasing speed. Bounded index
and contact signs therefore do not supply the missing integer selection.
The complete manuscript and independent review preserve the distinction
between this structural obstruction and the full strict-failure premise.

Root also derived an arbitrary-dimension counterfamily to the proposed
reciprocal-margin induction. Append distinct speeds `198h+1` to `(1,6,12)`.
Every tuple has margin `1/3` and exactly two normalized global maximizing
times, `7/18` and `4/9`. Their folds retain every distinct speed and have
margins `2/5` and `4/11`, respectively. Both violate the proposed inequality.
This excludes repairs based solely on taking a larger count or avoiding
zero deletion; the examples themselves all satisfy LRC. The argument is
an exact hand proof for every dimension, rather than an inference from
finite numerical success.

The initial small numerical checker located the three-speed example.
Root found a separate generic endpoint omission: a folded tuple can become
all odd after zero removal, and then time `1/2` must be considered. The
original receipt is retained alongside the corrected checker and its
fresh bounded execution. The hand counterfamily is independent of this
program and of the numerical search's prefix ordering.

Both mathematical manuscripts passed independent Astra/xhigh review without
corrections; root read the complete authors and reviews. The corrected
checker passed its endpoint and returned-example regressions, then found
the same first counterexample after 33 tuples in the unchanged finite
domain. Its observed terminal receipt records exit zero under a 512 MiB
address-space limit. Root checked the complete source change and receipt
bindings without repeating the numerical run. One receipt-only repair
separated program-written status from actual terminal observations;
the original checker defect and original execution remain preserved.

The next mathematical requirement remains a uniform mechanism using the
actual strict minimal-failure hypotheses. In the nonzero-fold branch,
neither the scalar norm bound nor real coordinate interpolation forces
an admissible integer improvement. Zero restoration is a separate live
analysis and is excluded from this checkpoint until fully reviewed.
No Lean build or dependency restoration is justified by these obstructions.

Pass 76 merged through PR75 at main commit
`27309c7e0a0d93c8f9ce90245cec013a3ffcd5d8`. Both CI checks passed, and
root verified the exact head and parent ancestry, three frozen files,
ten input bindings, and the verification receipt on fetched remote main.

During an interruption, the user reported system-wide memory exhaustion.
Kernel records and the separate Kiyori task history identified concurrent
Rust compilation of its patched Codex CLI as the dominant consumer.
That build was not restarted here. This pass uses manuscript review and
small Python checks; the project has no retained Lean build cache.
